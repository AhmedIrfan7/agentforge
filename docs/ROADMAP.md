# AgentForge Implementation Roadmap

300 steps, one commit each, pushed immediately after (see `AGENTS.md` §10 — Git Commit Discipline). Grouped into 14 milestones matching the GitHub Milestones this repo should carry. Do not batch steps into large commits; do not skip ahead without completing/testing the prior step.

**Stack locked in for this roadmap (see `docs/adr/0001-technology-stack.md`):**
Python + FastAPI (backend) · Next.js/React (dashboard) · vanilla TS (embeddable widget) · PostgreSQL + pgvector (data + vectors) · Redis + Celery (cache/queue/background jobs) · MinIO/S3 (object storage) · LangGraph (agent orchestration) · Docker Compose (local dev) · pnpm workspaces + uv (package management) · single monorepo.

Every provider-facing piece (LLM, embeddings, speech, vector store) sits behind an abstraction interface per `AGENTS.md` — swapping providers later must never require a rewrite.

---

## Milestone 0 — Foundation (steps 1–34)

- [x] 001. Init monorepo structure: `apps/`, `packages/`, `docs/`, `infra/`
- [x] 002. Add root `.gitignore`
- [x] 003. Add root `README.md` skeleton
- [x] 004. Add `LICENSE` (Apache 2.0)
- [x] 005. Add `CODE_OF_CONDUCT.md`
- [x] 006. Add `CONTRIBUTING.md` skeleton
- [x] 007. Scaffold `apps/api` FastAPI project (uv init)
- [x] 008. Add `pyproject.toml` with base deps (fastapi, uvicorn)
- [x] 009. Add ruff config for API — ruff only, not ruff+black (see step 009 commit; ruff format is black-compatible, running both was redundant)
- [x] 010. Add mypy config for API
- [x] 011. Add pytest config + first smoke test
- [x] 012. Scaffold `apps/web` Next.js project
- [x] 013. Add ESLint + Prettier config for web
- [x] 014. Add TypeScript strict config
- [x] 015. Add `pnpm-workspace.yaml`
- [x] 016. Scaffold `packages/shared` TS types package
- [x] 017. Add `docker-compose.yml` (Postgres+pgvector, Redis, MinIO) — config validated, not yet boot-tested (see 033)
- [x] 018. Add `.env.example` for api and web
- [x] 019. Add Makefile for common dev commands (`make` unavailable locally to test; commands verified directly)
- [x] 020. Configure pre-commit hooks (lint/format on commit)
- [x] 021. Add GitHub Actions workflow: lint
- [x] 022. Add GitHub Actions workflow: backend tests
- [x] 023. Add GitHub Actions workflow: frontend tests (build-check; no test suite exists yet)
- [x] 024. Add `GET /health` endpoint in API
- [x] 025. Add `docs/ARCHITECTURE.md` skeleton
- [x] 026. Add `docs/adr/` folder + ADR template
- [x] 027. Write ADR-0001: technology stack decision
- [x] 028. Write ADR-0002: monorepo vs multi-repo decision
- [x] 029. Write ADR-0003: multi-tenancy isolation strategy decision
- [x] 030. Add structured logging (structlog) in API
- [x] 031. Add centralized config module (pydantic-settings) in API
- [x] 032. Add Dockerfiles for api and web (multi-stage) — verified with real `docker build` + `docker run` for both apps (see step 033)
- [x] 033. Verify `docker compose up` boots api+web+db+redis+minio end-to-end — postgres+redis+minio all healthy via `docker compose up -d`, pgvector extension confirmed loadable; `apps/api` and `apps/web` Docker images built and run, both served real HTTP responses (`/health` → 200, web root → 200). Fixed a real bug found in the process: `apps/api/.python-version` was pinned to 3.14 (a local-machine workaround) which broke the container build against the `python:3.12-slim` base — repinned to 3.12.
- [x] 034. Add CI status badge to README

## Milestone 1 — Multi-Tenancy Core (steps 35–59)

- [x] 035. Add SQLAlchemy async engine setup
- [x] 036. Add Alembic migrations setup
- [x] 037. Create `Organization` model + migration
- [x] 038. Create `Workspace` model + migration — landed with 042+043, see their notes
- [x] 039. Create `User` model + migration — global identity, no auth fields yet (Milestone 2)
- [x] 040. Create `Membership` model (user↔org/workspace + role) + migration — tenant-scoped, RLS forced, two partial unique indexes for org-level vs workspace-level
- [x] 041. Create `Role`/`Permission` tables + migration — global catalog, 10 built-in roles seeded; Permission rows deliberately empty until Milestone 2 defines what they check
- [x] 042. Add `tenant_id` column + index convention to base model mixin — pulled forward before 038 (ADR-0003 requires tenant_id land in the same migration as the table, not bolted on after)
- [x] 043. Enable Postgres Row-Level Security policies on tenant tables — pulled forward with 042/038 for the same reason. **Found and fixed a real bug: the docker-compose bootstrap Postgres role is always a superuser and unconditionally bypasses RLS.** App runtime now uses a separate least-privilege `agentforge_app` role (see `infra/postgres/init/01-app-role.sql`, ADR-0003); migrations still use the bootstrap superuser. Verified with real cross-tenant read/write attempts against the actual least-privilege connection, not the superuser one.
- [x] 044. Add tenant-context dependency (`dependencies/tenant.py`) — the "resolve from JWT" half is an explicit `NotImplementedError` placeholder until Milestone 2's auth exists (deliberately not a client-header shortcut, see ADR-0003); the session/RLS-wiring half is real and tested end-to-end through FastAPI's dependency chain
- [x] 045. Add repository base class enforcing `tenant_id` filter on every query — `repositories/base.py`, `TenantScopedEntity` mixin added for clean generic typing
- [x] 046. Add test: cross-tenant query attempt is blocked — get/list/create all covered across `test_repository_base.py` and `test_tenant_isolation.py`
- [x] 047. Create Organization CRUD endpoints — fully functional, verified live against a running server (create/get/list/409/404/422/delete)
- [x] 048. Create Workspace CRUD endpoints — correctly wired but non-functional until Milestone 2 auth exists; verified both the fail-closed 500 and the working path via dependency override
- [x] 049. Add pagination utility for list endpoints — `schemas/common.py` (`Page[T]`, `PaginationParams`), `repositories/base.py:count()`
- [x] 050. Add API error-handling middleware (consistent schema, no internal leak) — `errors.py`
- [x] 051. Add Pydantic request/response schemas for org/workspace endpoints — `schemas/organization.py`, `schemas/workspace.py`
- [x] 052. Add integration tests for org/workspace endpoints — `test_organization_endpoints.py`, `test_workspace_endpoints.py`, 27/27 passing
- [x] 053. Add local dev seed script (demo org/workspace/user) — idempotent, verified live
- [x] 054. Add `AuditLog` table + model — `tenant_id` deliberately has no FK (outlives the org it's about)
- [x] 055. Add audit logging hook on org/workspace create/update/delete (no update endpoints exist yet, so create+delete only)
- [x] 056. Document tenant isolation approach in `docs/ARCHITECTURE.md`
- [x] 057. Add dedicated tenant-isolation pytest module — `test_tenant_isolation.py` (built alongside step 043)
- [x] 058. Add DB connection pooling config — `config.py`, applies outside tests only
- [x] 059. Add CI check: `alembic upgrade head` runs clean — plus `alembic check` for model/migration drift

## Milestone 2 — Authentication & Authorization (steps 60–81)

- [x] 060. Add password hashing utility (argon2)
- [x] 061. Create email/password signup endpoint
- [x] 062. Create login endpoint issuing JWT access+refresh tokens — found+fixed weak default JWT secret, added production startup guard against placeholder secrets
- [x] 063. Add refresh-token rotation endpoint — single-use rotation, replay of an old token correctly rejected
- [x] 064. Add logout/session revocation
- [x] 065. Add email verification flow — shared `VerificationToken` model (reused by 066, 067); found+fixed a real migration hazard against the seeded dev DB (NOT NULL column add needs server_default)
- [x] 066. Add magic-link login flow
- [x] 067. Add password reset flow — confirm revokes all other active sessions for the user, not just the password
- [x] 068. Add `Session` model (device/IP/last-active tracking) — built alongside step 061, since login needed it to issue refresh tokens against
- [x] 069. Add Redis-backed rate limiting on auth endpoints — per-IP fixed-window, disabled under test env (test suite legitimately exceeds real-world limits), verified live (3-request limit correctly 429'd on the 4th)
- [x] 070. Add RBAC permission-check FastAPI dependency — `dependencies/rbac.py:require_permission()`, union of permissions across every role the caller holds in the tenant
- [x] 071. Define role matrix (Super Admin, Org Owner, Admin, Manager, Knowledge Manager, Developer, Support Agent, Analyst, Viewer, End User, Guest) — seeded by migration `1d0ef14faf9e` (permission catalog + role→permission matrix)
- [x] 072. Enforce role checks per route via permission dependency — organization/workspace routes now require real JWT + membership + permission; found+fixed a real RLS bug (dependencies never set tenant context before querying Membership, so every check silently 403'd) and a real tenant-isolation leak (`list_organizations` returned all orgs unfiltered) along the way
- [x] 073. Add `Invitation` model + invite-teammate endpoint — `POST /organizations/{id}/invitations`, gated by new `invitation:create` permission (org_owner/admin/manager); opaque 7-day token (SHA-256 hashed, same pattern as VerificationToken); partial unique index blocks duplicate pending invites per (tenant, email)
- [x] 074. Add invitation-accept endpoint — `POST /invitations/accept`, token-only (no org in path); requires real auth + caller's email to match the invite; creates the Membership directly. Found and fixed a real production bug along the way: `tenant_isolation`'s RLS policy cast `''::uuid` on a reused pooled connection whose GUC had reverted from a prior request's `SET LOCAL` — invisible under pytest's NullPool, only surfaced live. Fixed with `NULLIF(..., '')` in `migrations/rls.py` and repaired every already-applied policy via migration `c5861fcf8c88`
- [x] 075. Add invitation expiration handling — `GET /organizations/{id}/invitations` (list, `invitation:read`) and `DELETE .../invitations/{id}` (revoke, `invitation:revoke`, idempotent, 409 if already accepted); `InvitationRead.status` derived from accepted_at/revoked_at/expires_at ("pending"/"accepted"/"revoked"/"expired") rather than stored; live-verified with real connection reuse, not just pytest
- [x] 076. Add Google OAuth login — `GET /auth/google/login`+`/callback`, server-side code exchange (no id_token re-verification needed), CSRF via double-submit state cookie, links-or-creates by Google-verified email, new `OAuthIdentity` table (not a `google_id` column, so step 077 doesn't need a schema change); found+fixed a real `Secure`-cookie-over-HTTP bug that would've broken every deployment not terminating TLS in the app process itself
- [x] 077. Add auth-provider abstraction interface (future SSO/SAML/OIDC) — `OAuthProvider` structural Protocol + `PROVIDERS` registry in `auth/oauth.py`; `routers/oauth.py` generalized to `/auth/{provider}/login`+`/callback` (Google's URLs unchanged); proved genuine via a test that registers an invented "github" provider and logs in through the same router with zero provider-specific branching
- [x] 078. Add MFA (TOTP) enrollment + verification — `POST /auth/mfa/enroll`+`/confirm`+`/disable` (real access token required) and `/verify` (redeems a short-lived `mfa_pending` JWT ticket for real tokens); TOTP secret encrypted at rest (Fernet), 10 argon2-hashed single-use backup codes; every primary-auth path (password/magic-link/Google OAuth) gated via a shared `complete_login()`, refresh exempt; found+fixed a real packaging bug (`httpx` only in dev deps, would've crashed a production Google-login the first time anyone tried it); live-verified with real TOTP codes end-to-end
- [x] 079. Add per-org `SecuritySettings` model (session timeout, password policy) — `GET`/`PATCH /organizations/{id}/security-settings` (org_owner+admin only), one row per org (auto-created + backfilled for existing orgs); `mfa_required` is the one field actually enforced (at `dependencies/tenant.py:get_current_tenant_id`, blocking every tenant-scoped route until the member enables MFA); session_timeout/password-policy fields are honest configuration-only for now — no clean enforcement point exists while User/Session stay global rather than org-scoped, documented directly rather than implied otherwise
- [x] 080. Add full auth-flow test (signup→verify→login→refresh→logout) — one continuous chain threading real data (signup's user id → its verification token → login's tokens → refresh rotation → logout revocation) through every step, catching integration gaps the existing per-endpoint tests' isolated fixtures can't; test-only, no new endpoints/models
- [x] 081. Add RBAC-enforcement tests (unauthorized role blocked) — systematic seeded role→permission matrix check (would catch a permission granted to the wrong roles, which every prior per-feature end_user-only test would miss) + real HTTP tests for previously-untested tier distinctions (admin vs org_owner-only delete, manager vs security_settings, viewer read-vs-write, guest's zero permissions). **Milestone 2 (Authentication & Authorization) complete — 81/300.**

## Milestone 3 — Knowledge Pipeline: Ingestion + RAG (steps 82–136)

- [x] 082. Create `KnowledgeBase` model + migration — sits under Workspace (org→workspace→knowledge base); deliberately minimal (no embedding/chunking config until steps 098-106 need it); also built minimal CRUD (create/list/get/delete, matching Workspace's own no-update scope) nested under `/organizations/{id}/workspaces/{id}/knowledge-bases`, since an inert model can't be reached/tested and step 084 needs a real target to upload into; `get_target_workspace` cross-checks the URL's workspace_id belongs to the resolved tenant (verified both cross-workspace and cross-org 404s)
- [x] 083. Create `Document` model + migration (status, metadata) — sits under `KnowledgeBase`; genuinely model-only (unlike 082) since step 084's file upload is what actually creates a Document, no throwaway create endpoint to build/discard; `status` plain str defaulting to "pending" (new pipeline stages are app-level additions, not migrations); `doc_metadata` JSONB (not `metadata` — reserved on every SQLAlchemy model); storage columns deliberately deferred to 084; tested at the ORM/RLS layer directly, no live-server check needed for a model-only step
- [x] 084. Add file upload endpoint (multipart → MinIO/S3) — `POST .../knowledge-bases/{id}/documents`; new `storage.py` (aioboto3, async S3-compatible client); Document gains `storage_key`/`content_type`/`size_bytes` (deferred from 083); deliberately no file-type/size validation (085/086) or virus scan (087) yet — real, tracked gaps, not hidden; live-verified with a real curl upload confirmed via `mc ls` in the bucket. CI needed a real MinIO service — `bitnami/minio:latest` turned out to not exist at all (confirmed via Docker Hub API, zero tags), fixed with a plain `docker run` step using the same `minio/minio:latest` image local dev uses, verified locally before repushing
- [x] 085. Add file-type allow-list validation (pdf, docx, pptx, xlsx, csv, txt, md, html, json, xml) — `validation.py`, two layers since neither extension nor client Content-Type is trustworthy: extension allow-list + magic-byte content check (`filetype`, pure Python) for pdf/docx/pptx/xlsx, UTF-8 decodability check for text formats (no binary signature exists for "this is text"); new `UnsupportedFileTypeError` (422, same `validation_error` code as pydantic validation); live-verified reject-bad-extension, reject-content-mismatch, and accept-real-signature all correct against a running server
- [x] 086. Add file-size limit validation — `config.max_upload_size_bytes` (50 MB default), enforced by `validation.py:read_upload_content` reading in bounded 1 MB chunks and bailing the moment the running total exceeds the limit, rather than buffering the whole upload first (which would defeat the point of a size limit); new `FileTooLargeError` (413, distinct from 422's validation_error); live-verified with `MAX_UPLOAD_SIZE_BYTES=10` against a running server, both reject and accept paths
- [x] 087. Add virus/malware scan step (ClamAV) before processing — `antivirus.py` speaks clamd's INSTREAM protocol directly over a raw `anyio` TCP socket (no client library — the only async one on PyPI, `aioclamd`, has had a single release since 2022); runs synchronously in the upload request itself since Celery doesn't exist until step 089, right after `validate_upload`/before `storage.upload_file`; new `InfectedFileError` (422, code `infected_file`, distinct from `validation_error` the same way `FileTooLargeError` got its own code); `docker-compose.yml`/CI gain a `clamav/clamav:1.5` service — the non-`_base` tag, chosen because it ships its virus database pre-loaded (no slow freshclam download on container start); caught a real mistake before it shipped: almost bind-mounted a host volume over `/var/lib/clamav`, which would've shadowed that pre-loaded database with an empty directory and forced the exact slow download the tag was chosen to avoid; live-verified against a real running server + real clamd — clean upload succeeds, the official EICAR test string is rejected with signature `Eicar-Test-Signature` (and got real-world confirmation the test bytes are genuine: Windows Defender quarantined the on-disk copy the moment it was written, had to pipe the content through curl's stdin instead)
- [x] 088. Add upload progress tracking (status field + polling) — `status` field itself already existed since step 083 with nothing new to add there (no processor exists yet to transition it); the real net-new piece is `GET .../documents/{id}/status`, a deliberately smaller sibling of the existing full `GET .../documents/{id}` (`DocumentStatusRead`: id/status/updated_at only) so a client polling for progress isn't re-fetching title/doc_metadata/content_type on every tick; reuses `document:read` (no new permission — same capability, smaller view); no model/migration changes, every future pipeline stage just sets `document.status` and it shows up here automatically; live-verified against a real running server (upload → poll → confirm smaller payload shape + 404 for unknown id)
- [x] 089. Set up Celery worker app + Redis broker config — `celery_app.py`, no real tasks yet (step 090 defines the first one); shares `config.redis_url` with the existing cache/rate-limiter connection rather than a second Redis; explicit `json` serializers, not Celery's pickle-capable default (RCE surface if the broker's ever compromised); dropped celery's own `[redis]` extra — its pinned redis version range conflicted with this project's already-declared `redis>=8.1.0` under uv, and was redundant anyway since redis is already a direct dependency; `ping` task exists only to prove the wiring, tested in-process via `Task.apply()` (no subprocess/broker in the automated suite — nothing dispatches a real task until 090, so a real round-trip belongs in live verification, not new CI flakiness for infra with no product surface yet); live-verified with a real worker process (`--pool=solo` needed on Windows dev, not Linux/CI) dispatching `ping.delay()` against real local Redis and getting `"pong"` back — bonus proof the worker survives a broker restart and reconnects on its own, discovered when Redis actually cycled mid-verification
- [x] 090. Add background task: content extraction dispatcher (per file type) — `extraction.py:dispatch_extraction`, the first real Celery task, dispatched from `upload_document` right after the Document row + audit log land; routes by extension through a plain `HANDLERS` dict (same shape as `auth/oauth.py:PROVIDERS`); only csv/txt/json/xml have a real handler (trivial UTF-8 decode genuinely IS the whole extraction for those — no structure to pull out the way there is from a PDF/DOCX); pdf/docx/pptx/xlsx/html/md land on the honest `"extraction_unsupported"` until 091-093 register theirs; new nullable `Document.extracted_text` (migration `b91be877b219`) for chunking (098+) to read later. **Three real bugs found live, not by inspection:** (1) worker never saw the task at all (`KeyError: 'dispatch_extraction'`) — a worker only knows tasks from modules it actually imported, `-A celery_app worker` alone doesn't import `extraction.py`; fixed with `celery_app.conf.imports` (not a direct import, which would be circular); (2) a second commit inside one task after `SET LOCAL app.current_tenant_id` had already lapsed silently filtered every row out of RLS's UPDATE policy → `StaleDataError` — the exact bug class step 074 found live, reproduced here for the identical reason; fixed by re-setting tenant context after the intermediate commit; (3) a second task in the same long-lived worker process crashed in asyncpg/proactor internals — `asyncio.run()` makes a new event loop per task, and a pooled connection bound to the first loop breaks when a later, different loop reuses it, the same "Event loop is closed" class `db.py` already avoids for pytest via `NullPool`; fixed with a new `db.py:get_worker_session` on its own `NullPool` engine for any process that creates a new loop per unit of work. Live-verified end-to-end with a real worker + real server: `.txt`/`.json` uploads poll through to `"extracted"` with correct `extracted_text`; `.pdf` lands on `"extraction_unsupported"` with `extracted_text` staying null.
- [x] 091. Add PDF text/structure extraction (headings, tables) — `extraction_pdf.py`, registered into `HANDLERS["pdf"]`; pdfplumber (MIT), not PyMuPDF/fitz (AGPL-3.0-or-commercial, a real license conflict with this Apache 2.0 project); font-size heuristic for headings (no PDF format has a native "this is a heading" concept) — two tiers (`#`/`##`) so a title and its section headings don't collapse together, verified against a real reportlab-generated PDF before trusting pdfplumber's actual word/table shape; detected tables become markdown pipe-tables, with table-bbox words excluded from line extraction and lines+tables interleaved back by vertical position so output reads in original page order without duplicating table contents as loose words; output is markdown deliberately — same convention step 093 (HTML/Markdown) and step 100 (markdown/heading-aware chunker) will share. Unit-tested against real generated PDFs (title→H1, heading2→H2, table→pipe-table no dup, reading order, empty-doc no crash) + one wiring test through the real dispatcher. Live-verified with a real worker + real server: uploaded a title+heading+table PDF, polled to `"extracted"`, confirmed `extracted_text` matched the source structure exactly.
- [x] 092. Add DOCX/PPTX/XLSX extraction — `extraction_docx.py`/`extraction_pptx.py`/`extraction_xlsx.py`, registered into `HANDLERS`; all three output markdown like pdf, same shared convention. python-docx: iterates the document body's raw XML children directly rather than the library's own flat `.paragraphs`/`.tables` lists — verified live those two lists silently drop the true relative order of a table sitting between two paragraphs; heading levels read straight from the paragraph's real style name ("Title"/"Heading N"), no heuristic needed since docx encodes them explicitly. python-pptx: a slide's title placeholder becomes a heading, other text frames become paragraphs, tables become markdown tables — verified live that a title-slide layout's title is `CENTER_TITLE`, not `TITLE`, so both are checked. openpyxl: `read_only`+`data_only`, each sheet becomes a heading plus one markdown table. `extraction_tables.py` promoted out of `extraction_pdf.py` once four extractors needed the identical rows-to-markdown logic — found and fixed a real bug in it along the way: `cell or ""` blanked out any cell whose real value was `0`/`False`/`""`, not just an actually-empty (`None`) cell, harmless for pdf's always-text cells but wrong for xlsx's typed ones; fixed to check `is None` specifically. Unit-tested against real docx/pptx/xlsx files built with each library's own writer (headings, tables, ordering, empty-doc no-crash) + three wiring tests through the real dispatcher. Live-verified with a real worker + real server: uploaded real docx/pptx/xlsx files (including a `0`/`False` xlsx cell) through the real HTTP endpoint, confirmed each `extracted_text` matched exactly what was authored.
- [x] 093. Add HTML/Markdown extraction — `extraction_html.py` registered into `HANDLERS["html"]`; markdownify (MIT), not html2text (GPL-3.0-or-later, a real license conflict with this Apache 2.0 project, same reasoning that ruled out PyMuPDF/fitz for pdf). Reconsidered markdown along the way: earlier docstrings assumed it would need its own structure-aware transformation like pdf/docx/pptx/xlsx; it doesn't, since markdown is already this pipeline's own target output format — an uploaded `.md` file's bytes already ARE the extracted content, same as csv/txt/json/xml, so `md` joined `HANDLERS` as a plain-text passthrough instead. Two things verified live before trusting them: markdownify's `strip=` option does not remove a tag's content, only its markdown formatting, so `<nav>`/`<footer>` text still leaked through — those plus `<header>`/`<aside>`/`<script>`/`<style>` are removed outright with BeautifulSoup's `.decompose()` first, an honest bounded cleanup based on unambiguous HTML5 semantics, not full "guess the article body" content extraction; and `table_infer_header` defaults to `False`, which breaks a table using plain `<td>` for its header row (a common real-world case) — set to `True`. Every extension `validation.py` allows now has a real handler; `"extraction_unsupported"` is defined but no longer reachable through any upload the API currently accepts. Live-verified with a real worker + real server: uploaded HTML with genuine nav/header/footer/aside/script/style noise plus a heading and a plain-`<td>` table, and a real markdown file — noise fully stripped, table converted correctly, markdown came back byte-for-byte unchanged.
- [x] 094. Add metadata-extraction step (title, author, dates, language) — `extraction_metadata.py:build_doc_metadata`, populates `Document.doc_metadata` in the same pass `dispatch_extraction` already runs (reuses the already-downloaded bytes/extracted text, no separate task). Per-format metadata lives next to each format's content-extraction function (docx/pptx `core_properties`, xlsx `workbook.properties` — its own `creator` field normalized to a common "author" key, pdf's own non-ISO-8601 date format parsed by hand, html `<title>`/`<meta name="author">`/`<html lang>`), returning raw uncleaned values; cleaning is centralized in `extraction_metadata.py` rather than duplicated five times. Two real sentinel-default gotchas found live before writing any cleaning logic: python-docx defaults an unset author to the literal string `"python-docx"`, openpyxl defaults an unset creator to `"openpyxl"` — neither a real name, both filtered; dates are deliberately NOT filtered the same way (both libraries default unset dates to their own fixed/dynamic values, not detectable via a sentinel), a known accepted limitation of trusting embedded document metadata. Language: an explicitly declared one (per-format field, or html's `lang` attribute) wins over statistical detection; falls back to py3langid (BSD) otherwise, using `norm_probs=True` — verified live that degenerate input (empty/garbage/single-char) converges to a low ~0.17 confidence versus ~1.0 for real text, making a confidence threshold meaningful rather than arbitrary; below it, language stays null rather than guessing wrong. Live-verified with a real worker + real server: docx with explicit title/author/created/language, pdf with explicit title/author (dates parsed from its real Info-dictionary timestamps), and a plain txt file for pure content-based language detection — all three came back with correct `doc_metadata`, including docx's undated `modified_at` correctly showing python-docx's own known 2013 default rather than a wrong value being silently hidden.
- [x] 095. Add Document Analysis Agent skeleton (classifies doc type) — `agents/document_analysis.py`, the first real agent (AGENTS.md SECTION 5); scoped to what the roadmap line actually asks (classification), not AGENTS.md's full aspirational responsibility list for this agent — table detection is already inherent to `extraction_tables.py` (091-093), duplicate-upload detection is its own later step (117). Classification is keyword-phrase scoring into faq/manual/legal/academic/business (falls back to `"general"`), not an ML model or LLM call — same "don't overclaim precision" stance as the PDF heading heuristic and the language-confidence threshold; the winning category's matched keywords are reported alongside the classification, explainable by construction rather than a fabricated confidence score. No shared `Agent` base class/registry yet despite AGENTS.md describing a full Agent Registry/Orchestrator architecture — mirrors how OAuth was built here (concrete Google implementation first, step 076; generalized into a real Protocol + registry only once a second provider existed to prove the abstraction was real, step 077) — extracting a shared shape from one agent would be designing against a guess; revisit once 097 (Chunking Recommendation Agent) lands. Wired into `extraction.py:_run_extraction` right after metadata extraction, same pass, reusing the already-in-memory `extracted_text`; result lands in `Document.doc_metadata` as `document_type`/`document_type_signals`. Live-verified with a real worker + real server: uploaded a real FAQ-formatted document, confirmed `doc_metadata` came back with `document_type: "faq"` and the matched signal.
- [x] 096. Add document quality checks (empty pages, broken formatting, duplicate hash) — `quality.py`, computed in the same extraction pass as steps 094/095, informational only (not upload-blocking — that's `validation.py`'s job at upload time). `content_hash` = SHA-256 of raw bytes, its own indexed `Document` column (not JSONB) so step 117 (duplicate-document detection) can query it efficiently later — this step only computes/stores the signal, doesn't act on it. `is_empty`/`has_broken_formatting` land in `doc_metadata`; `is_empty` is whole-document not literally per-page (no extractor currently returns a per-page breakdown, only one combined string — honest about that rather than claiming page-level detection); `has_broken_formatting` flags >5% Unicode replacement/control characters, threshold verified live against clean vs. deliberately corrupted text. Live-verified with a real worker + real server: two files with identical content under different names produced identical `content_hash` (plus a SHA-256("") sanity check on an empty upload), and a genuinely empty file correctly flagged `is_empty: true`.
- [x] 097. Add Chunking Recommendation Agent skeleton (scores strategies + explains choice) — `agents/chunking_recommendation.py`, the second real agent; scores five candidates (fixed_size, sentence_paragraph, markdown_heading, table_aware, recursive_hybrid) from real structural signals in the extracted markdown (heading/table lines, paragraph breaks, length) — heuristic, not ML/LLM, same stance as the PDF heading heuristic. All five get scored, not just the winner (AGENTS.md's own "compare them, score them" framing). None of the five have a real chunker yet (098-102) — the recommendation itself is this step's deliverable, for step 103's override endpoint. Extracted `agents/base.py`'s minimal `Agent` (just `name`, no shared `run()`) now that a second real agent exists to compare against 095's — the two share an input type but do genuinely different things through differently-named methods (`analyze` vs `recommend`), so `name` (for independent logging) is the only honest shared contract. Real bug caught by testing: `recursive_hybrid`'s first scoring version could only ever tie a single-strategy score at the 1.0 ceiling, never beat it, so a genuinely mixed document (strong tables AND headings) lost the tie to `table_aware` instead of recommending the hybrid strategy built for that exact case — fixed by deriving `recursive_hybrid`'s score from the already-computed table/heading scores and switching the winner selection to `>=` with `recursive_hybrid` listed last, so it wins precisely the tie against the strategies it exists to combine. Wired into `extraction.py:_run_extraction`, same pass as 094-096. Live-verified with a real worker + real server: uploaded a real table-heavy document, confirmed the stored recommendation correctly picked `table_aware` with all five scores and matching reasoning.
- [x] 098. Implement fixed-size chunker — `chunking_fixed_size.py`, first of the five strategies `agents/chunking_recommendation.py` (097) scores among; pure algorithm only, nothing wired in yet (`Chunk` isn't a DB model until step 105, no real dispatch until 099-102 and 104 also land). Character-based, not token-based — no tokenizer/embedding provider exists yet (107) to tie a token budget to. 1000-char chunks by default, snapped back to the nearest whitespace so a chunk doesn't end mid-word (basic hygiene, not 099's sentence/paragraph awareness), falls back to a hard cut only when no whitespace exists within the lookback window — verified live in a REPL against a pathological 5000-char single "word" that it degrades gracefully rather than hanging. 200-char overlap between consecutive chunks, standard RAG practice. No server/API touches this yet, so unit tests (empty/short/multi-chunk/overlap/coverage/indices/word-boundary/pathological-word/reconstruction, 9 cases) are the real verification, not a live curl check.
- [x] 099. Implement sentence/paragraph-aware chunker — `chunking_sentence_paragraph.py`, second of five strategies `agents/chunking_recommendation.py` scores among; pure algorithm, nothing persisted yet (`Chunk` isn't a DB model until step 105). Packs whole paragraphs together when they fit, falls back to whole sentences for an oversized paragraph, and only an individual oversized sentence falls all the way back to `chunk_fixed_size`'s raw character splitting — composing the two chunkers instead of duplicating word-safe splitting logic. `Chunk` itself promoted out of `chunking_fixed_size.py` into a new `chunking_types.py` now that a second chunker needs the identical shape, same "build inline for the first consumer, promote once a second needs it" pattern `extraction_tables.py` already used. Sentence splitting is a real, bounded regex heuristic, not NLP — verified live first; known, accepted limitation on abbreviations ("Dr. Smith" splits after "Dr."), which doesn't meaningfully hurt chunk quality since the stray fragment gets packed back in with its neighbors. Real bug caught by testing, not inspection: paragraph offset tracking via `text.find("\n\n", ...)` silently dropped an extra newline when a document had more than one blank line between paragraphs, corrupting every later offset — fixed by switching to a regex scan over runs of blank-line whitespace.
- [x] 100. Implement markdown/heading-aware chunker — `chunking_markdown_heading.py`, third of five strategies `agents/chunking_recommendation.py` scores among; same pure-algorithm scope as 098/099. Splits along `#` through `######` heading boundaries (matching every extractor's own output shape), packs small adjacent sections together, and an oversized section falls back to `chunk_sentence_paragraph` for its own content — composing rather than re-implementing a third packing pass. Known, accepted limitation: when an oversized section sub-splits, only its first resulting piece carries the heading text — `Chunk.text` staying an exact substring of the source (same invariant 098/099 hold) mattered more than repeating the heading into every fragment. Promoted the packing algorithm itself into a new `chunking_packing.py` once it turned out byte-for-byte identical to `chunking_sentence_paragraph.py`'s own — same "build inline for the first consumer, promote once a second needs it" pattern already used for `Chunk` and `rows_to_markdown`; both chunkers now share one implementation.
- [x] 101. Implement table-aware chunker — `chunking_table_aware.py`, fourth of five strategies `agents/chunking_recommendation.py` scores among; same pure-algorithm scope as 098-100. Splits text into alternating table/prose regions (contiguous `| ... |` line runs vs everything else); prose regions get `chunking_sentence_paragraph.py`'s own paragraph/sentence splitting, composed not reimplemented. Tables become one atomic unit each, no matter how large — deliberately never split a table across a chunk boundary, since a mid-row cut destroys its meaning far more than an oversized chunk costs; verified live that a table well over `chunk_size` still comes back as exactly one whole chunk. First chunker in this pipeline where atomicity deliberately wins over the size cap for a specific unit type. Reuses `chunking_packing.py:pack_units`, same shared implementation the two chunkers before it use. One unrelated test (`test_organization_create_and_delete_are_audited`) flaked on the first full-suite run, passed clean on an isolated re-run and a full re-run — confirmed transient, not a regression, before treating the suite as green.
- [x] 102. Implement recursive/hybrid chunker — `chunking_recursive_hybrid.py`, fifth and last strategy `agents/chunking_recommendation.py` scores among; same pure-algorithm scope as 098-101. Genuinely hybrid, not just another single technique: splits by heading section first, then only within oversized sections protects tables as atomic units and packs surrounding prose by paragraph/sentence — combining what the markdown-heading chunker (respects sections, doesn't protect tables) and table-aware chunker (protects tables, no heading concept) each handle alone but not together. Matches exactly what the recommendation agent scores this strategy for: documents with both strong table and strong heading signals at once. Verified live against a table sitting inside an oversized section — came back completely whole. Promoted `split_sections`/`split_table_and_prose_regions` out of the markdown-heading and table-aware chunkers into a new shared `chunking_regions.py` since this one needed both — same "build inline for the first consumer, promote once a second needs it" pattern used throughout this pipeline. All five chunking strategies now exist as pure, fully-tested algorithms.
- [x] 103. Add chunking-strategy override endpoint (accept/override recommendation) — `PATCH .../documents/{id}/chunking-strategy`. One endpoint covers both actions: the caller always states the strategy they want, compared against the agent's own recommendation (if any) to decide the stored `source` ("accepted"/"override") — no need for two separate actions on one resource. New `document:update` permission (org_owner/admin/manager, same tier as `document:create/read`) — first document-mutation permission beyond create. `agents/chunking_recommendation.py`'s strategy tuple made public (`STRATEGY_NAMES`) so the router validates against the same source of truth, not a duplicated list; unknown name → 422 listing real allowed values. Only records the decision in `doc_metadata` for now — step 104 promotes to real `Document` columns, nothing dispatches real chunking against it yet (step 105 creates `Chunk` first). Live-verified against a real running server: no-recommendation override, accepting a real recommendation, overriding one (reasoning correctly names the original recommended strategy, not the previous decision), rejecting an unknown name, 404 for a nonexistent document, and confirmed each successful call wrote its own audit log entry.
- [x] 104. Persist chunking decision + reasoning on `Document` — `chunking_strategy`/`chunking_strategy_source`/`chunking_strategy_reasoning` real columns, promoted out of step 103's `doc_metadata["chunking_decision"]`. `doc_metadata["chunking_recommendation"]` stays JSONB (genuinely multi-field diagnostic scores); the single chosen strategy gets real columns instead. `extraction.py` now sets a real default the moment a recommendation exists (`source="recommended"`); the override endpoint updates the same columns to `"accepted"`/`"override"`. Three states, not two — "unreviewed" is meaningfully different from "human-confirmed," useful for an eventual admin queue. Comparison for accept/override still reads the ORIGINAL recommendation from `doc_metadata`, not the current column (which changes as overrides happen) — verified live that overriding twice in a row still correctly names the true original recommendation both times. Surfaced all three on `DocumentRead` (unlike `extracted_text`/`content_hash`, deliberately not exposed) since there's no dedicated GET for just the decision. Live-verified end to end with a real worker + server: real extraction set the recommended default, accept, then double-override, all correct.
- [x] 105. Create `Chunk` model + migration — mirrors `chunking_types.py:Chunk`'s shape (`text`/`start`/`end`/`index`) plus tenant-scoping and `document_id` FK (CASCADE). RLS enabled and verified directly against Postgres (`relrowsecurity = t`), not just assumed from calling `enable_rls()`. `(document_id, index)` unique. No embedding columns yet (106-109's job), no repository either — genuinely model-only, same precedent Document itself set at step 083, tested directly at the ORM/RLS layer (create/read, ordering, unique-constraint rejection, cascade-delete). Real, honest gap documented in the model's own docstring: nothing dispatches chunking yet — no code path creates a `Chunk` row from a document's `extracted_text` + `chunking_strategy`; the roadmap has no step between this one and 108 ("batched embedding generation") dedicated to that dispatch. Real bug caught before shipping: the first `alembic revision --autogenerate` came back completely empty — `Chunk` wasn't registered in `models/__init__.py`, which `migrations/env.py` relies on (`import models`, not per-file discovery) for autogenerate to see any model at all. Deleted the empty stub, fixed the registration, regenerated for real.
- [x] 106. Add embedding-provider abstraction interface — `embeddings/base.py:EmbeddingProvider`, a structural Protocol matching `auth/oauth.py:OAuthProvider`'s own precedent (self-contained adapter per provider, no shared inheritance). Deliberately sequenced interface-first, unlike OAuthProvider which stayed concrete through one implementation before generalizing — this roadmap puts the abstraction (106) before any real provider (107), so there's no earlier single-implementation step to generalize away from, and no `PROVIDERS` registry yet either, matching this project's "don't build machinery before there's something real to put in it" discipline. `embed()` is batch-shaped (`list[str] -> list[list[float]]`) from the start since step 108's own name ("batched embedding generation") already commits to batching being how the pipeline calls a provider. `dimensions` is a real required property, not speculative — step 109's pgvector column needs a fixed vector size at table-creation time. No error-handling contract specified beyond "async, can raise" — left for step 107's real OpenAI adapter to define from an actual failure mode, not guessed at here. Tested with a real dataclass implementation (`_FakeEmbeddingProvider`), not a mock, proving the Protocol shape actually works the way it promises. Unrelated to this step: diagnosed and fixed a local-only test-DB hygiene issue — `test_chunk_model.py` uses fixed slugs and real commits with no rollback/truncate fixture, so re-running the file in isolation and then the full suite back-to-back (both against the same local Postgres container) collided on leftover rows from the first run; confirmed via CI's workflow file that this can't happen there (fresh service-container Postgres per run) — not a code regression, just local dev-DB state, cleaned up with a targeted `DELETE ... WHERE slug LIKE 'chunk-%'`.
- [x] 107. Implement OpenAI embedding provider — `embeddings/openai.py:OpenAIEmbeddingProvider`, the first real `EmbeddingProvider`. Speaks OpenAI's REST `/v1/embeddings` endpoint directly over `httpx`, same "direct authenticated server-to-server call, no vendor SDK needed for one endpoint" reasoning `auth/oauth.py:GoogleOAuthProvider` already established. Verified the real request/response JSON shape (headers, `model`/`input`/`dimensions` fields, `data[].index`/`embedding`, `usage`) against OpenAI's own API docs before writing any code against it. `text-embedding-3-small` chosen as the model — cheapest current OpenAI embedding model, 1536 native dimensions, matched exactly by `dimensions = 1536` on the class (no truncation `dimensions` param sent, since 1536 already is this model's native size). Deliberately no `PROVIDERS` registry, unlike OAuth's own step 077 — that registry only earned its keep once a SECOND real provider existed to prove the abstraction was real; nothing later in this roadmap ever adds a second embedding provider (150-152/216-218 are the LLM/speech interfaces, not this one), so a registry here would be permanent machinery with nothing to ever hold. Response rows sorted by `index` defensively rather than trusting list order. New `EmbeddingProviderError` (plain `Exception`, not `errors.py`'s `AppError`) is the one thing any failure mode (auth, rate limit, network, malformed response) translates to — deliberately not an `AppError` since this runs inside a Celery task, not a FastAPI request/response cycle, so there's no HTTP status code to carry. **Honest, documented gap**: no real `OPENAI_API_KEY` is configured in this environment, so real live verification against OpenAI's actual API wasn't possible — request/response/error logic is instead verified with `httpx.MockTransport` (built into httpx, no new dependency), which exercises the provider's real code path end to end against a realistic (if fake) response, same spirit as `SecuritySettings`' own honestly-unenforced-fields precedent (step 079) rather than silently skipping or falsely claiming live verification. Whoever adds a real key later should do one real live call to close this gap for good.
- [x] 108. Add background task: batched embedding generation — `embeddings_pipeline.py:dispatch_embedding_generation`, dispatched automatically from `extraction.py:dispatch_extraction` right after a successful extraction (same "one stage's success kicks off the next" shape `upload_document` already uses). Genuinely folds in two other steps' literal scope, both by real necessity, both explicitly documented here and in the code: (1) **closes step 105's long-documented gap** — nothing dispatched chunk creation before this; `CHUNKERS` maps `agents/chunking_recommendation.py:STRATEGY_NAMES` to the five real chunker functions (098-102), same plain-dict-registry shape as `auth/oauth.py:PROVIDERS`/`extraction.py:HANDLERS`; (2) **folds in step 109's own literal task** (`Chunk.embedding`, pgvector `Vector(1536)` + ivfflat cosine-ops index) ahead of its number, since computing a real, paid embedding with nowhere durable to store it would mean discarding a real OpenAI response or inventing a throwaway column just to redo it a step later — both worse than building the already-fully-specified (by 106/107) schema now. `CREATE EXTENSION vector` added manually to the migration (nothing in this repo had installed the extension before now, despite Milestone 0 confirming it was merely *loadable*). Texts batched in groups of 100 per `EmbeddingProvider.embed()` call — large enough to be efficient, small enough that one failed batch (112's future job to retry) doesn't waste an entire large document's already-computed embeddings. **Known, accepted, explicitly-documented tension, not silently resolved**: this fires immediately with no window for step 103's override endpoint to act first; re-chunking after an override isn't built (`models/chunk.py` already flagged this as a future concern), so overriding after this task runs keeps the old chunks/embeddings. Live-verified end to end against a real server + real worker + real Postgres: upload → real extraction → automatic dispatch → correctly failed closed with `document.status="embedding_failed"` and **zero orphaned `Chunk` rows** (no real `OPENAI_API_KEY` configured in this environment — the `httpx.LocalProtocolError: Illegal header value b'Bearer '` failure is the expected, correctly-handled shape of that gap, not a bug). The success path (chunk creation, batching, vector storage, status→`"embedded"`) is verified for real against Postgres — real inserts through the least-privilege `agentforge_app` role, not the migrations superuser — with a fake `EmbeddingProvider` swapped in via `tests/test_embeddings_pipeline.py`, same reasoning `test_google_oauth_endpoints.py:FakeOAuthProvider` already established for swapping a whole provider rather than mocking calls inside a real one.
- [x] 109. Add pgvector column + index on `Chunk` — completed as a necessary, explicitly-documented part of step 108's own scope (see that entry) rather than redone here: `Chunk.embedding` is `pgvector.sqlalchemy.Vector(1536)`, nullable, plus `ix_chunks_embedding_ivfflat` (`ivfflat`, `vector_cosine_ops`, `lists=100`) — chosen over `hnsw` as this project's default first ANN index (cheaper to build, good enough at the row counts a real deployment starts at); tuning/rebuilding it as data grows is explicitly left to step 110's own background task, not this migration's job to get right forever. Verified directly against real Postgres, not just assumed from the migration succeeding: `\d chunks` confirms the column type and index, `\dx` confirms the `vector` extension is actually installed, and `alembic check` reports zero drift.
- [x] 110. Add background task: vector indexing — `vector_maintenance.py:reindex_chunk_embeddings`, `REINDEX INDEX CONCURRENTLY` on `ix_chunks_embedding_ivfflat`. A genuinely distinct concern from 108's embedding generation, matching AGENTS.md's own "DOCUMENT PIPELINE OBSERVABILITY" list, which names "Embedding generation" and "Vector indexing" as separate stages: 108 computes and stores one vector per chunk; this task keeps the ANN index itself healthy as those vectors accumulate. Verified against pgvector's own documented guidance before building this: ivfflat centroids are computed once, at build/reindex time, from whatever data exists in the table right then — an index built (or last reindexed) against an empty or unrepresentative sample keeps degraded recall *permanently*, not temporarily, until rebuilt against real data. `ix_chunks_embedding_ivfflat` was created by 108/109's migration against an empty `chunks` table, so it starts in exactly that degraded state by pgvector's own stated failure mode, not a hypothetical future concern. `REINDEX CONCURRENTLY` specifically (not a plain `REINDEX`, which exclusive-locks the table for its duration) — cannot run inside a transaction block, so this uses AUTOCOMMIT isolation on a dedicated connection, verified live before trusting it. **Real, load-bearing finding, live-verified, not assumed:** running this as the app's normal least-privilege `agentforge_app` role fails — `ERROR: must be owner of index` — REINDEX requires index ownership on Postgres 16 (the `MAINTAIN` privilege that would let a non-owner role do this doesn't exist until Postgres 17). Fixed by routing this one task through `settings.database_migrations_url` (the bootstrap/superuser connection Alembic already uses) instead — `config.py`'s docstring updated to document this as a narrow, honest, deliberate second use, not a silent boundary violation: REINDEX touches no tenant data, so it isn't a meaningful security regression against this project's least-privilege stance. No scheduler dispatches this automatically — the roadmap never adds Celery Beat or any periodic-task infrastructure, so "background task" here means a real, dispatchable Celery task like every other one in this codebase; who/what triggers it (pgvector's own guidance: monthly/quarterly, or after a large ingestion burst) is real operator/ops work this roadmap doesn't ask for yet. Tested against real Postgres, not mocked — a mocked connection would hide exactly the permission failure this step actually found.
- [x] 111. Add ingestion-pipeline status endpoint (per-stage visibility) — `GET .../documents/{id}/pipeline-status`, richer than step 088's single-string `/status`: a per-stage breakdown (`pipeline_status.py:compute_pipeline_stages` — extraction/chunk_generation/embedding_generation) plus real `Chunk` counts (`repositories/chunk.py`). Deliberately doesn't claim all eight of AGENTS.md's named pipeline stages: upload/validation both happen synchronously before a `Document` row even exists, `vector_indexing` (step 110) is a property of the shared ivfflat index across every chunk, not per-document state, and "publication" has no real concept anywhere in this codebase yet (no draft/published distinction exists) — reporting fake state for any of those would be worse than the honest three-stage view this ships instead. `chunk_generation` in particular is grounded in a real, queried `Chunk` count, not inferred from `Document.status` alone, since `extraction.py` and `embeddings_pipeline.py` can be mid-transition between each other. Live-verified end to end against a real server + worker: a document that fails at the embedding stage (same no-`OPENAI_API_KEY` gap steps 107/108 already documented) correctly shows `extraction: completed`, `chunk_generation: failed`, `embedding_generation: failed`, `chunk_count: 0` — matches the real all-or-nothing per-document commit behavior 108 already proved.
- [x] 112. Add retry/backoff handling for failed pipeline stages — `dispatch_extraction`, `dispatch_embedding_generation`, and `reindex_chunk_embeddings` (110) all now retry with exponential backoff (`autoretry_for`/`retry_backoff`) up to their existing `max_retries=5` ceiling. **Real, load-bearing gap found and fixed, not assumed:** `max_retries=5` had been set on `dispatch_extraction`/`dispatch_embedding_generation` since their own introduction (090/108), but nothing ever actually called `self.retry()` or configured `autoretry_for` for a genuine pipeline failure — only `dispatch_extraction`'s narrow `_DocumentNotFoundYet` race (a flat 1s retry, unrelated to real failures) ever retried anything. A real extraction or embedding failure previously got exactly one attempt, no retry, despite `max_retries` implying otherwise. Confirmed via `celery/app/autoretry.py`'s own source that `self.retry()`'s `Retry` exception passes through `autoretry_for`'s wrapper untouched (`except Retry: raise`), so the existing manual `_DocumentNotFoundYet` handling and the new blanket `autoretry_for=(Exception,)` compose safely without double-counting. **Known, accepted, explicitly documented limitation:** this doesn't distinguish a transient failure (worth retrying) from a permanent one (a file that will never parse, an invalid API key) — both burn the full retry budget before landing on their terminal failed status; a real retryable-vs-permanent classification is real future work, not built here. Verified two ways: `Task.apply()` eager-mode tests proving actual retry-then-succeed and retry-then-give-up behavior (confirmed live first that eager retries don't sleep in wall-clock time, so this stayed fast — 4 tests, under 2 seconds), and a real worker against real Redis, watching `dispatch_embedding_generation` genuinely retry with real backoff delays (1s, 0s, 2s, 7s, 2s observed) through 5 attempts before failing closed on the same no-`OPENAI_API_KEY` gap 107/108/111 already documented.
- [x] 113. Add end-to-end ingestion test (fixture PDF → chunks+embeddings) — `test_ingestion_end_to_end.py`, one continuous chain with real data threaded through, same reasoning `test_full_auth_flow.py` (step 080) already established for this project's other milestone-closing integration test. A real reportlab-generated PDF, uploaded through the real HTTP endpoint, run through the real `dispatch_extraction` → `dispatch_embedding_generation` task chain (`Task.apply()`, no live worker needed — same technique `test_pipeline_retry.py`/112 already proved fast and reliable), asserted against real `Chunk` rows with real pgvector embeddings, cross-checked against the pipeline-status endpoint (111). **Real bug caught while writing this, not assumed:** calling `dispatch_extraction.apply()` from inside an `async` pytest-anyio test raises `"asyncio.run() cannot be called from a running event loop"` — the task itself calls `asyncio.run()` internally, written to run from a synchronous Celery worker context, not an already-running loop. Fixed by keeping the test function itself synchronous and running each async DB-verification step through its own short-lived `asyncio.run()` call instead of sharing one event loop with the `.apply()` calls. No real `OPENAI_API_KEY` exists in this environment (same gap 107/108/111/112 already documented) — `embeddings_pipeline._embedding_provider` is swapped for a fake so this test genuinely reaches `"embedded"`, the real terminal state the step's own name asks for ("chunks+embeddings"), rather than honestly-but-uselessly stopping at `"embedding_failed"`. This test doesn't need separate manual live verification — it already exercises real Postgres, real MinIO, and real Celery task execution end to end, the same infrastructure a live-verification pass would otherwise check by hand.
- [x] 114. Add document re-index endpoint (on update/replace) — `POST .../documents/{id}/reindex`, re-dispatches `embeddings_pipeline.py`'s existing `dispatch_embedding_generation` task (no new Celery task needed) against a document's current `chunking_strategy`. Closes a real, previously documented tension flagged repeatedly since step 108: overriding `chunking_strategy` (step 103) never used to actually regenerate a document's chunks — this is that missing trigger, explicit rather than automatic, so a caller reviews/overrides first and re-indexes deliberately, not a silent re-chunk on every override. `_run_embedding_generation` now deletes existing `Chunk` rows for the document right before inserting the new ones — only once the new embeddings are already computed and ready, so a failed re-index attempt leaves the previous working chunks untouched rather than risking a document with zero chunks; without this, a second dispatch for the same document would crash on the `(document_id, index)` unique constraint instead of replacing anything. 409, not 404/422, for a document with no `chunking_strategy` set yet — same "right resource, wrong state" reasoning `Invitation`'s already-accepted 409 uses elsewhere. `document:update`, the same permission the chunking-strategy override endpoint already requires. Live-verified end to end against a real server + worker: a document that already failed once (no `OPENAI_API_KEY`, the standing documented gap) got a real second embedding attempt on reindex — confirmed via a changed `updated_at` and a fresh retry sequence in the worker log — and never accumulated orphaned chunks across either attempt.
- [x] 115. Add document versioning (replace preserves history) — `POST .../documents/{id}/versions` replaces a document's content: snapshots the CURRENT `Document` fields into a new `DocumentVersion` row first, then updates the document to the new upload and dispatches a fresh extraction cycle, same validation/antivirus pipeline as the original upload. `GET .../documents/{id}/versions` lists history, newest first. `DocumentVersion` snapshots `Document`'s own fields, not `Chunk` rows — chunks belong to whatever content is CURRENT (step 114 already established a document has exactly one live chunk set at a time), so "preserves history" means being able to see what a document used to be, not a parallel chunk-versioning system. The new upload gets a fresh, randomized `storage_key` rather than reusing `upload_document`'s `{tenant}/{kb}/{document_id}/{filename}` convention — reusing it here (same `document_id`, likely same filename) would overwrite the OLD object in MinIO/S3, defeating "preserves history" at the storage layer before a version row could even point at it. **Real bug caught live, not assumed:** reading `document.updated_at` right after `session.flush()` on an UPDATEd (not freshly-inserted) row raised `MissingGreenlet` — `updated_at` is server-computed (`onupdate=func.now()`), and unlike `upload_document`'s freshly-INSERTed row (whose defaults populate automatically), an updated row's `onupdate` value isn't known in-memory after `flush()` alone. Fixed with an explicit `session.refresh()` before returning the response. `document:update` for replacing content, `document:read` for listing history — both existing permissions. Live-verified end to end against a real server: uploaded a document, replaced its content, confirmed the new upload's fields took over, confirmed a real `DocumentVersion` row preserved the old title/size, and confirmed both storage objects existed independently in MinIO.
- [x] 116. Add tenant-scoped document deletion (cascades chunks+embeddings) — `DELETE .../documents/{id}`. `Chunk` (105) and `DocumentVersion` (115) rows cascade automatically at the DB level (`ondelete="CASCADE"` foreign keys) — nothing new needed there. The real remaining gap was storage: neither Postgres FKs nor anything else in this codebase cleans up MinIO/S3 objects (115's own live verification surfaced this same gap for org deletion). This endpoint closes it for a document's own storage footprint — deletes the document's current object plus every `DocumentVersion`'s own historical object, via new `storage.py:delete_file`. Storage cleanup runs BEFORE the DB delete, not after — if it fails partway, the `Document`/`DocumentVersion` rows and their audit trail stay intact for a retry or investigation, rather than the DB row disappearing first and leaving now-undiscoverable orphaned objects in MinIO. New `document:delete` permission (org_owner/admin/manager, same tier as `document:create/read/update` and `knowledge_base:delete`) — a distinct grant from `document:update` since deleting is genuinely more destructive/harder to undo, even at the same role tier; updated `test_rbac_enforcement.py`'s `EXPECTED_ROLE_PERMISSIONS` matrix, same drift-catching pattern every new permission in this project has triggered. Tested against real Postgres and real MinIO, not mocked: uploads, replaces content, adds a real `Chunk` row, confirms both storage objects exist, deletes, confirms the `Document`/`Chunk`/`DocumentVersion` rows and both MinIO objects are all genuinely gone — this test's own real-infra assertions are the live verification for this step, no separate manual pass needed (same call step 113 already made).
- [x] 117. Add duplicate-document detection within a knowledge base — wires `quality.py`'s own `content_hash` (096) into a real detection step: the same extraction pass now queries `repositories/document.py:DocumentRepository.list_duplicates_in_knowledge_base` for other documents in the same knowledge base sharing that hash, adding `doc_metadata["duplicate_document_ids"]` (a list of string UUIDs, always present, empty when there are none). Informational only, same "detect, don't block" stance `quality.py`'s own checks already take — `validation.py`/`antivirus.py` already accepted the file at upload time, long before extraction runs. Scoped to one knowledge base, matching the roadmap step's own wording — the same file genuinely uploaded into two different knowledge bases isn't the kind of accidental duplicate this step means to catch. Only documents that already finished extraction (`content_hash` already committed) can match, so two uploads of the identical file racing each other correctly never flag one another before either has a real hash to compare. Tested against real Postgres: identical content in the same KB flags the second upload against the first (and only the second), different content doesn't flag, identical content in a different KB under the same tenant doesn't flag either. Live-verified end to end against a real server + worker: two real uploads of identical content, confirmed the second correctly named the first's document id. **Session note:** this step's live verification spanned a machine/Docker restart (usage-limit gap) — recovered by re-verifying from scratch per this project's standing lesson: confirmed the Docker engine, all 4 containers, and the Postgres data volume all survived and matched expectations before trusting anything from before the gap, then re-ran the full suite fresh before treating it as green.
- [x] 118. Add `VectorStore` abstraction interface — `vectorstore/base.py:VectorStore`, a structural Protocol matching `embeddings/base.py:EmbeddingProvider`'s own step-106 precedent: interface-first, since this roadmap sequences the abstraction (118) before any concrete implementation (119's pgvector adapter). `upsert()` and `search()` are the two halves a genuine store needs, not just a searcher — a swapped-in implementation later needs its own way of representing "these chunks live here too," not only a way to query what's already there. Both tenant- and knowledge-base-scoped explicitly, matching this project's consistent defense-in-depth tenant-isolation discipline. No `PROVIDERS` registry, and this one is never expected to grow one — checked the full roadmap for a second vector-store step and there isn't one (ADR-0001 already locked pgvector in as the only vector store), same call already made for `embeddings/openai.py` at 107. No metadata-filtering parameter on `search()` yet, even though step 123 is already known to need one — same "add the field when the step that needs it lands" discipline used throughout the ingestion pipeline, rather than guessing at a filter shape before a real caller exists. Tested with a real fake implementation (`_FakeVectorStore`), not a mock, same reasoning `test_embedding_provider.py` already established. Pure interface, no HTTP/DB touch — no live-server verification needed, same as step 106's own precedent.
- [x] 119. Implement pgvector `VectorStore` adapter — `vectorstore/pgvector.py:PgVectorStore`, the real (and only) implementation this roadmap ever builds. Wraps the already-existing `chunks` table and its ivfflat index (105/108/109/110) behind the `VectorStore` interface — no new schema. `search()` uses pgvector-python's `Chunk.embedding.cosine_distance(...)`, verified live against real Postgres before trusting it — confirmed it compiles to the real `<=>` operator the index's own `vector_cosine_ops` expects. Scoped by `tenant_id` (app-layer filter on top of RLS) and `knowledge_base_id` via a join through `Document`, since `Chunk` has no `knowledge_base_id` column of its own. Uses `db.py:get_worker_session` (NullPool), not `get_session` — this provider has no fixed caller yet (120's retrieval endpoint is the first), so it can't assume the single persistent event loop `get_session`'s pooled engine requires. **`upsert()`'s real, honest limitation, documented rather than worked around:** `VectorRecord` deliberately doesn't carry `start`/`end`/`index` (not every vector store backend has that concept), but `Chunk`'s own table requires them — this adapter's `upsert()` is therefore update-only in practice, raising a clear error rather than silently no-op-ing for a nonexistent id, since nothing in this roadmap ever needs it to create a chunk from scratch outside `embeddings_pipeline.py`'s own dispatch. Tested against real Postgres, not a fake store: real cosine-distance ranking, `top_k`, knowledge-base scoping, chunks without embeddings correctly excluded, both `upsert` paths. No HTTP endpoint exists yet (that's step 120) — these real-infra tests are this step's own live verification.
- [x] 120. Add dense (vector similarity) retrieval endpoint — `POST .../knowledge-bases/{id}/search`, the first real caller of `PgVectorStore.search()` and `OpenAIEmbeddingProvider.embed()` (119/107). A free-text query is embedded with the same provider that embedded the chunks, then searched via the real pgvector adapter. Promoted `get_target_knowledge_base` out of `routers/document.py` into `dependencies/knowledge_base.py` — `routers/retrieval.py` is a second real consumer of the identical cross-check logic, same "build inline, promote once a second needs it" pattern used throughout this pipeline; `routers/document.py` now imports the shared version. Deliberately minimal response shape (`chunk_id`/`document_id`/`text`/`score`, no title or metadata) — this is the plumbing-proving step, not the polished search surface; step 133 owns that once reranking/context-building/citations (125-127) exist to justify a richer shape. Reuses `knowledge_base:read`, not a new permission. Own module-level `EmbeddingProvider` instance rather than reaching into `embeddings_pipeline.py`'s underscore-prefixed attribute from a different module. No real `OPENAI_API_KEY` exists in this environment (same gap as 107/108/111-114/117) — live-verified that a real search request fails closed with a clean 500, no internals leaked. The success path is tested end to end through the real HTTP stack (real auth, real RBAC, real Postgres, real pgvector cosine ranking) with a fake embedding provider swapped in.
- [x] 121. Add keyword/full-text retrieval (Postgres tsvector) — `POST .../knowledge-bases/{id}/search/keyword`, a genuinely separate retrieval mechanism from dense/vector search, not an extension of `VectorStore` (confirmed at 118's own design). `Chunk.search_vector` is a DB-computed column (`GENERATED ALWAYS AS to_tsvector('english', text) STORED`) — verified live before trusting it (`sqlalchemy.Computed` compiles to the real Postgres syntax; a real `to_tsvector`/`plainto_tsquery`/`ts_rank` round trip matched correctly through the least-privilege `agentforge_app` role). Deriving it at the DB level guarantees it can never drift out of sync with `text`. New GIN index — the standard type for tsvector search, unrelated to `embedding`'s own ivfflat index. `ChunkRepository.search_by_keyword` scoped the same way `PgVectorStore.search()` is (tenant_id + join through `Document` for `knowledge_base_id`); `plainto_tsquery`, not `websearch_to_tsquery`/raw `to_tsquery` — plain natural-language input, no operator syntax a caller needs to know. `schemas/retrieval.py`'s `DenseSearchRequest`/`DenseSearchResultRead` renamed to `SearchRequest`/`SearchResultRead` now that keyword search is a second real consumer of the identical shape — a Python class rename only, not a wire-format change. No embedding step, so unlike dense search this works for real in every environment, including one with no `OPENAI_API_KEY` — the full test suite (real Postgres, real HTTP stack, real RBAC) is this step's own live verification.
- [x] 122. Add hybrid retrieval combining dense+keyword — `POST .../knowledge-bases/{id}/search/hybrid`, calls both real retrievers (120/121) and fuses their results with Reciprocal Rank Fusion rather than being a third independent mechanism. Verified RRF is the real, widely-adopted technique for exactly this problem before committing to it — Elasticsearch's own `rrf` retriever, OpenSearch, Weaviate, and Qdrant's hybrid search all use it, since it operates on rank position rather than raw score (cosine similarity and `ts_rank` aren't on comparable scales, so weighted combination would need a normalization step this project would otherwise have to invent). `k=60`, the standard smoothing constant from the original paper and every production system's own default, not chosen freely. `retrieval_fusion.py:reciprocal_rank_fusion` is a pure function, unit-tested against the real RRF formula directly. Each underlying retriever queried for `HYBRID_CANDIDATE_POOL_SIZE` (50) results regardless of the caller's own `top_k`, then fused and truncated — fusing only within each retriever's own narrow `top_k` risks missing a chunk that ranks well in one but just outside the other's. Hybrid's `score` is the fused RRF score, not a similarity/rank score in dense/keyword's own sense — documented as an intentional departure. Tested end to end through the real HTTP stack: a chunk matching both signals correctly outranks one that only ranks well in dense alone, proving RRF actually drives the ranking. Same `OPENAI_API_KEY` gap as dense search (120) for the failure path — already live-verified there, not repeated here since it's the identical mechanism/failure mode.
- [x] 123. Add metadata filtering in retrieval queries — `document_id` and `document_type` filters, added to all three retrieval endpoints. Deliberately not a speculative generic filter DSL, but the two real, populated fields this codebase can actually answer queries about today. `document_id` scopes to one document; `document_type` comes from `agents/document_analysis.py`'s own classification (095), already in `Document.doc_metadata`. `vectorstore/base.py:VectorStore.search()` closes the deferral 118 explicitly flagged, via a new `SearchFilters` dataclass. `PgVectorStore.search()` applies `document_type` through `Document.doc_metadata["document_type"].astext` — verified live before trusting it (SQLAlchemy's JSONB accessor compiles to the real Postgres `->>` operator). `ChunkRepository.search_by_keyword` gets the same two filters as plain keyword arguments, not a shared dataclass — it isn't a `VectorStore` implementation. The interface-level fake (`test_vectorstore_base.py`) only tests `document_id` — `VectorRecord` deliberately doesn't carry `document_type` (not every vector-store backend would have it as a record-level concept), so `document_type` filtering is instead tested directly against real Postgres, where `PgVectorStore`'s real join-through-`Document` implementation genuinely has access to it. Tested against real Postgres for both filters on both mechanisms, plus one HTTP-level test proving the request body's `document_id` reaches the real endpoint's real query — filtering logic itself already proven at the layer below.
- [x] 124. Add Retriever Agent skeleton wrapping retrieval logic — `agents/retriever.py:RetrieverAgent`, matching AGENTS.md's own Agent Registry vision now that dense/keyword/hybrid (120-123) all exist as real, working, tested logic to wrap. A genuine "extract, don't rewrite" step: `search_dense`/`search_keyword`/`search_hybrid` are the exact logic that previously lived inline in `routers/retrieval.py`'s three endpoint functions, moved unchanged so the router becomes thin wiring (request in, agent call, schema out). The first agent that's async and talks to real collaborators (`EmbeddingProvider`, `VectorStore`, a session-scoped `ChunkRepository`) rather than a pure function over in-memory text, unlike `document_analysis.py`/`chunking_recommendation.py` (095/097) — `name` still exists purely for AGENTS.md's "independent logging" tag, not a forced shared `run()` shape. Own `RetrievedChunk` return type (`chunk_id`/`document_id`/`text`/`score`), not `schemas.retrieval.SearchResultRead` — confirmed no module under `agents/`, `repositories/`, or `vectorstore/` imports from `schemas/` anywhere in this codebase before choosing this, since `schemas/` is a router-facing (FastAPI `response_model`) layer an agent has no business depending on. `ChunkRepository` passed in per-call, not held on the agent, since it's built from a request-scoped `AsyncSession`; the agent itself is a module-level singleton in `routers/retrieval.py`, same shape as the pre-step-124 `_embedding_provider`/`_vector_store` singletons it replaces. Tested with fakes for `search_dense` (same "prove the Protocol contract" reasoning as `test_vectorstore_base.py`) and against real Postgres for `search_keyword`/`search_hybrid` (same "no mocks for infrastructure this project owns" reasoning as `test_chunk_repository_keyword_search.py`); `search_hybrid`'s own test combines one real fake-dense result with one real Postgres-keyword result to prove fusion works through the agent, not re-proving either half's own correctness again. `test_retrieval_endpoints.py`'s monkeypatches retargeted from the old module global to `routers.retrieval._retriever_agent._embedding_provider`, since the provider is now bound into the agent instance at construction time rather than read fresh from a module global per request. Live-verified end to end over real HTTP: a real chunk injected via the ORM, `search/keyword` returned it correctly through the full router→agent→repository→Postgres path, and `search`/`search/hybrid` failed closed with a clean 500 at the same documented `OPENAI_API_KEY` gap as before extraction — proving the router's dense/hybrid paths still reach the agent correctly even without a real embedding provider.
- [x] 125. Add reranking step (abstracted provider) — `rerankers/base.py:RerankerProvider`, a structural Protocol matching `embeddings/base.py:EmbeddingProvider`/`vectorstore/base.py:VectorStore`'s own interface-first precedent. No registry — checked the full 126-300 range for a second reranking step and there isn't one, same call already made for `embeddings/openai.py` and `vectorstore/pgvector.py`. `RerankCandidate`/`RerankResult` are this package's own minimal shapes (`id`/`text` in, `id`/`score` out), not `agents.retriever.RetrievedChunk` — `rerankers/` sits below `agents/` in this codebase's layering (same as `embeddings/`/`vectorstore/`), so it must not import from it; a caller maps down before calling and back up after. `rerankers/lexical.py:LexicalReranker` is the one real, concrete implementation: recall-oriented term-overlap scoring (`|query ∩ candidate| / |query|`) — a genuine, honest heuristic, not an ML cross-encoder or LLM call, same "don't overclaim precision" stance as `agents/chunking_recommendation.py`/`agents/document_analysis.py`. Deliberately a different signal from both of this project's other two real ranking mechanisms: not `ts_rank` (frequency/position-weighted within one tsvector) and not cosine similarity (semantic, never looks at literal text) — a plain "does this candidate actually contain the query's words" cross-check. Zero external dependency, unlike every other real provider in this codebase — works for real in every environment, including this one, with no fake needed for a live test. AGENTS.md's own "Design reranking as an independent stage" taken literally: wired into `agents/retriever.py:RetrieverAgent` as a new, separate `rerank()` method operating on results a caller already has, not folded into `search_dense`/`search_keyword`/`search_hybrid` (each of which already produces its own real ranking) — `reranker` is a per-call parameter, not a constructor-held singleton, since no router step yet always wants reranking applied. Tested at three layers: the interface with a real fake (`test_reranker_base.py`, same "real implementation, not a mock" reasoning as every other provider fake), `LexicalReranker`'s actual scoring behavior directly (`test_lexical_reranker.py` — term coverage, case-insensitivity, recall-not-precision behavior on a long candidate, empty-query division-by-zero guard), and `RetrieverAgent.rerank()`'s own wiring (`test_retriever_agent.py`, real `LexicalReranker`, no fakes needed). No HTTP endpoint yet and no live-server verification needed — pure library code with no router touching it yet, same precedent as step 118's own interface-only step.
- [x] 126. Add context-builder module (dedupe, order, token-budget aware) — `context_builder.py`, scoped to this line's own three concrete asks, not AGENTS.md's full aspirational "CONTEXT BUILDING" list (citation preservation and the richer document/section/page reference system are step 127's own job; "remove irrelevant information" is already retrieval/reranking's job, 120-125). A standalone pure module, same shape as `retrieval_fusion.py`, not a `RetrieverAgent` method — no async I/O, a deterministic transform. Own `ContextCandidate`/`ContextChunk` types, not `agents.retriever.RetrievedChunk` — same layering rule `rerankers/base.py` established at 125 (this module sits below `agents/`, must not import from it); `ContextChunk` deliberately drops `score`, since a context chunk's real output is the text an LLM prompt receives, not a ranking signal from an earlier stage. Dedupe: keeps the first occurrence of each normalized (trimmed, lowercased) text, dropping later duplicates — input is already relevance-ordered by the caller, so the first copy is always the best-ranked one, no extra score bookkeeping needed. Order: groups chunks from the same document adjacent to each other, preserving the order each document first appeared in and each document's own chunks' relative order within its group — a documented, honest limitation, not true in-document positional order (`Chunk.index`), since `RetrievedChunk` doesn't carry `index` today and no real caller needs it yet. Token budget: real counts via `tiktoken`, `text-embedding-3-small`'s own `cl100k_base` encoding (verified live before trusting it) — the one real model this codebase currently integrates, not a guess at a not-yet-chosen chat model (steps 150+); character-count estimation was considered and rejected since a real, free, offline measurement was available. New dependency added to `pyproject.toml`/`uv.lock`; its one real cost is a network fetch of its BPE ranks file on first use (verified live, cached after), no bigger a dependency than `uv sync` already needing PyPI in CI. Truncation stops at the first candidate that doesn't fit rather than skipping ahead to a smaller later one, preserving the grouped order rather than silently breaking it. Tested with plain pure-function unit tests (dedupe, case/whitespace-insensitivity, grouping across interleaved documents, budget-boundary truncation, zero-budget, empty-input). No HTTP endpoint yet and no live-server verification needed — pure library code with no router touching it yet, same precedent as steps 118/125.
- [x] 127. Wire citation-tracking through retrieval→context→response — `citations.py`, read the same way steps 125/126 read their own roadmap lines: build the real, correct primitive now, since no response (chat/generation) step exists yet in this codebase to wire into (that's steps 150+) — no live endpoint this connects to today, same "no HTTP endpoint yet" position as 118/125/126. A standalone pure module, same shape as `retrieval_fusion.py`/`context_builder.py`: `build_citations()` takes `document_info`, a plain `dict[uuid.UUID, DocumentInfo]` lookup the caller already has or fetches via the existing `DocumentRepository.get()`/`KnowledgeBaseRepository.get()` — not a new repository method of its own, since nothing calls this yet. Page references (AGENTS.md's own "where available" qualifier) are deliberately not a `Citation` field at all — confirmed by reading `extraction_pdf.py` that every PDF page gets joined into one continuous markdown string with no page boundary retained anywhere on `Document`/`Chunk` to honestly cite. Section references ARE derivable, conditionally: `_extract_section` parses a chunk's own leading markdown heading line directly out of its real text (verified live before trusting the regex) — `chunking_markdown_heading.py`'s own documented limitation means only a section's first resulting chunk keeps its heading, so any other chunk honestly gets `section=None`, not a fabricated one. `document_info[chunk.document_id]` is plain dict indexing, not `.get()` with a placeholder fallback — a missing entry is a real caller bug, not a legitimate "unknown document" state, so it fails loud (`KeyError`) rather than fabricating display text, same precedent as `PgVectorStore.upsert()`'s own error-not-silent-noop choice. Tested with plain pure-function unit tests: real name propagation, heading extraction (present at position 0, absent, present but not at position 0), the missing-lookup-raises case, multiple chunks each getting their own citation, empty input. No HTTP endpoint yet and no live-server verification needed — pure library code with no router touching it yet, same precedent as 118/125/126.
- [x] 128. Add RAG evaluation harness (precision/recall on labeled fixtures) — new `eval/` package, scoped to this line's own concrete ask, not AGENTS.md's full "RAG EVALUATION" list (citation accuracy/groundedness/answer quality/user satisfaction all need a real response/chat step to exist, steps 150+, same gap already documented at 127). `eval/metrics.py`: pure `precision_at_k`/`recall_at_k`, the standard IR formulas, operating on plain `uuid.UUID`s — no dependency on any retrieval-specific type, same layering rule as `rerankers/`/`context_builder.py`/`citations.py`. `eval/fixtures.py`: a small, real, hardcoded corpus (5 documents, distinct topics) — query wording was iterated LIVE against real Postgres before finalizing, since `ChunkRepository.search_by_keyword`'s `plainto_tsquery` ANDs every non-stopword term together; a first-draft query ("how long does shipping take") matched nothing at all because "long" never appears anywhere in the target document, not just ranked lower — a real, live-caught bug, not a hypothetical. `eval/harness.py`: `seed_fixture_set()` (seeds real Postgres) and `run_eval()` (scores a caller-supplied `query -> list[document_id]` closure) are deliberately separate functions — `run_eval` can't build a mechanism-specific search closure itself without depending on `agents/`/`repositories/`, breaking the same layering rule; the caller (a test today, a future endpoint eventually) binds its own tenant_id/session/repo into the closure using the ids `seed_fixture_set` returns. Scores at document granularity (each fixture document becomes exactly one real Chunk, so document- and chunk-level relevance coincide here). This IS the step's own live verification — `test_rag_eval_harness.py` runs the harness for real against real `ChunkRepository.search_by_keyword` (works in every environment, unlike dense search's `OPENAI_API_KEY` gap) and confirms mean precision/recall are a real 1.0 on the well-separated fixture set, plus that a search returning nothing scores a real 0 rather than being silently averaged away. No HTTP endpoint yet — same "no live-server verification needed" position as 118/125/126/127.
- [x] 129. Add retrieval-quality logging (query, results, scores, latency) — every `agents/retriever.py:RetrieverAgent` `search_*`/`rerank` method now logs one structured event via `logging_config.py`'s existing `get_logger(__name__)` convention (module-level `logger`, snake_case event name — same shape `errors.py`/`main.py`/`notifications/email.py` already use). `_log_retrieval` is one small private helper shared by the three `search_*` methods (identical field shape each time); `rerank()` logs its own event separately since its field set genuinely differs (no `knowledge_base_id`, a `reranker` name instead). Query text is logged verbatim, not hashed or redacted — this codebase has no existing PII-redaction convention for logged user input (`notifications/email.py` already logs a real recipient email address the same way), so inventing a new, inconsistent redaction policy just for retrieval queries was rejected. `search_hybrid`'s own event times the whole operation (both real sub-searches plus fusion); its two sub-searches each log their own event too, an honest reflection of the real work actually done, not suppressed as noise. Tested with `structlog.testing.capture_logs()` (verified live before trusting it) — real structured-logging output, not a mocked call — one test per method confirming the right event name and fields, plus one confirming `search_hybrid` emits all three real events in order. Live-verified end to end: hit the real `/search/keyword` HTTP endpoint against a running server and confirmed the structured `retrieval_keyword_search` log line appeared in real server stdout with real `query`/`result_count`/`latency_ms`/`knowledge_base_id` fields.
- [x] 130. Add multi-query retrieval strategy — `multi_query.py:expand_query`, one of the named techniques in AGENTS.md's own "RETRIEVAL STRATEGY" section ("evaluate multiple retrieval techniques... combine methods when beneficial"). Query expansion is a real, deterministic heuristic, not an LLM call — no chat/generation step exists yet in this codebase (steps 150+) and no real `OPENAI_API_KEY` exists in this environment either, same "honest heuristic when a real ML/LLM technique doesn't exist yet" stance as `agents/chunking_recommendation.py`/`agents/document_analysis.py`/`rerankers/lexical.py`. Splits a compound query ("refund policy and shipping times") on conjunctions/commas into its own clauses, returned alongside the original unsplit query; a simple, non-compound query expands to only itself — one variant, one retrieval call, never slower than calling the underlying search directly. Capped at 5 variants so a pathological query can't fan out unbounded. A real, live-caught bug fixed before finalizing: an Oxford-comma list ("X, Y, and Z") left a stray leading "and" on the last clause (`"and warranty coverage"` instead of `"warranty coverage"`) — caught by testing the exact real-world phrasing pattern, not a hypothetical. Wired in as `agents/retriever.py:RetrieverAgent.search_multi_query`, which runs a caller-supplied `Callable[[str], Awaitable[list[RetrievedChunk]]]` once per variant and fuses the per-variant result lists with the same `reciprocal_rank_fusion` `search_hybrid` already trusts — the same "caller binds its own mechanism-specific collaborators into a plain closure" pattern `eval/harness.py` established at step 128, since multi-query retrieval is orthogonal to which mechanism (dense/keyword/hybrid) actually runs; this method has no business picking one itself. Also logs its own `retrieval_multi_query_search` event (step 129's convention). Tested with pure unit tests for `expand_query` (simple/compound/Oxford-comma/case-insensitivity/cap/no-real-split cases) and with a fake in-memory search closure proving `search_multi_query` calls once per variant and fuses correctly, without needing real Postgres. No router wiring yet (this method has no HTTP caller today) — no live-server verification needed, same precedent as 118/125-128.
- [x] 131. Add parent-child chunk retrieval strategy — `repositories/chunk.py:ChunkRepository.get_expanded_context`, the "chunk window expansion" variant of parent-child retrieval: rather than a separately stored, differently-sized "parent" chunk (a genuine schema change — checked the full 132-300 range before writing this and nothing else in this roadmap ever needs it), a matched chunk's wider context is reconstructed on the fly from its own real neighbors, using `Chunk.index` (sequential position within its document, populated by every chunking strategy since step 105) and `Chunk.document_id` — both already real, correct data, no new columns or migration. `window` chunks on each side, ordered back into their original document sequence and joined with a blank line. Raises for a nonexistent `chunk_id` rather than silently returning an empty string, same "fail loud on a caller bug" precedent as `citations.py`'s dict-indexing (127) and `PgVectorStore.upsert()`'s (119). Wired in as `agents/retriever.py:RetrieverAgent.expand_to_parent`, a thin wrapper — same "independent, opt-in stage" shape as `rerank()` (125), not folded into `search_dense`/`search_keyword`/`search_hybrid`, since forcing every result to fetch its expanded context would be a real behavior/performance change nobody asked for. Tested against real Postgres: window inclusion, clamping at a document's start without error, `window=0` returning just the target chunk, and — the one case worth calling out — same-index chunks in two *different* documents never bleeding into each other's expansion (proves the `document_id` scope, not just `index` proximity, is what actually bounds the window). One thin wiring test on the agent side; the real logic is already proven at the repository layer. No router wiring yet — no live-server verification needed, same precedent as 118/125-130.
- [x] 132. Add tenant-isolation test for retrieval — `tests/test_retrieval_tenant_isolation.py`, extending `test_tenant_isolation.py`'s own established RLS-proof pattern (single session, `flush()` not `commit()`, switch tenant context mid-test) to the `chunks` table specifically, which that general test never covered (only `Workspace`). Tests two distinct threats: (1) a raw `select(Chunk)` with no app-layer `tenant_id` filter at all, under another tenant's RLS context — proves RLS alone, not just the repository's own explicit filter; (2) the real retrieval entry points (`ChunkRepository.search_by_keyword`/`get_expanded_context`, `PgVectorStore.search`) called under tenant A's own `tenant_id` but with tenant B's genuinely REAL, valid `knowledge_base_id`/`chunk_id` — materially stronger than the existing per-endpoint 404 tests (`test_retrieval_endpoints.py`), which only ever probe a random, never-real UUID, not an id that legitimately exists but belongs to someone else. `PgVectorStore.search()` needed its own exception to the flush-only discipline: it opens its own session via `get_worker_session()` (step 119), a different connection than a test's own `get_session()`, so flushed-but-uncommitted rows are invisible to it — that one test commits and does its own explicit per-model cleanup (same shape `test_retrieval_endpoints.py`'s own `_cleanup_org` already established), honestly documented as the one exception in the file's own docstring. All 4 tests passed on the first real run — tenant isolation already held across every retrieval mechanism built in 120-131, confirming rather than fixing.
- [x] 133. Add knowledge-base search API endpoint — `POST .../knowledge-bases/{id}/context` (`routers/retrieval.py`), the polished, product-facing search surface `SearchRequest`/`SearchResultRead`'s own docstrings promised back at steps 120-122 once reranking/context-building/citations existed to justify a richer response shape. `schemas/retrieval.py:ContextSearchRequest`/`ContextResultRead` are deliberately their own new schemas, not `SearchRequest` extensions — this endpoint's real job is composing four already-built pieces into one response, none of which the three plumbing-proving endpoints (120-122) should grow just because a fourth exists: strategy choice (`dense`/`keyword`/`hybrid`, default `hybrid`), optional multi-query expansion (`search_multi_query`, 130), optional reranking (`rerank`, 125), `context_builder.build_context` (dedupe/group/token-budget, 126), and `citations.build_citations` (127) — the first real caller of all four at once. `document_info` is built with a plain per-document `DocumentRepository.get()` loop, not a new bulk-fetch method — the candidate set reaching this point is already bounded by `max_tokens` (typically a handful of distinct documents), so nothing here honestly justified new repository machinery with no other real caller yet. Parent-child expansion (`expand_to_parent`, 131) is deliberately NOT wired in — it changes what a chunk's own `text` even means (a wider window, not the original match) in a way that would need to happen before token-budgeting, not after, a real design question the roadmap's own terse wording doesn't settle, so it's left out rather than guessed at. `ContextResultRead` drops `score`, same "own real output shape, not carried-over ranking signal" reasoning `citations.py:Citation` already established. Tested end to end through the real HTTP stack: real citations (document title, KB name, section parsed from a chunk's own leading markdown heading) for the keyword strategy (no `OPENAI_API_KEY` needed), dense strategy wired correctly via a fake embedding provider, the token budget actually truncating results, `rerank`/`multi_query` flags not erroring (proving wiring, not re-deriving their own already-tested correctness), and the standard 404/permission checks. Live-verified end to end over real HTTP: a real injected chunk returned the correct document title, knowledge base name, and section through the full pipeline; dense strategy failed closed with the same documented `OPENAI_API_KEY` gap as the existing endpoints.
- [x] 134. Add tenant-scoped retrieval caching (Redis) — caches `/context`'s response (`routers/retrieval.py`, step 133) in `redis_client.py`'s existing shared client, the same one `rate_limit.py` already uses (`celery_app.py`'s own comment had already anticipated this exact "cache" use case). Cache key hashes `tenant_id` + `knowledge_base_id` + every request field (sha256 over canonical JSON, not string concatenation, which risks ambiguous field boundaries) — "tenant-scoped" per this line's own wording, so two tenants issuing the identical query text never collide. A real, stated limitation, not silently worked around: a flat 5-minute TTL is the only invalidation mechanism, no hook into document reindex/delete (114/116) — the roadmap's own line doesn't ask for reindex-aware invalidation, so one wasn't guessed at. Disabled under `settings.environment == "test"`, same reasoning and precedent as `rate_limit.py`: `redis_client` is a module-level singleton whose connection binds to whichever event loop touches it first, and every one of `test_retrieval_endpoints.py`'s ~20 existing/unrelated tests would otherwise touch it for the first time via `/context`, risking connections leaking across the many separate event loops pytest-anyio creates. Real caching is proven in its own dedicated file (`tests/test_retrieval_caching.py`) with `environment` patched to `"production"`, calling `build_search_context()` directly rather than through `TestClient` — a real bug found and fixed while writing it: Starlette's `TestClient` runs the ASGI app inside its own internal event loop, and mixing that with the outer test function's own loop raised a genuine `RuntimeError: Event loop is closed` the moment a second request tried to reuse the Redis connection from the wrong loop; `test_rate_limit.py`'s own test file had already independently arrived at the identical "call the dependency directly, not through HTTP" workaround for the same underlying reason. Tests prove three things for real: a second identical call returns the first's (by-then-deleted-from-Postgres) data, proving Redis is genuinely read, not just written to and ignored; a different query is never served from another query's cache entry; two tenants issuing the identical query never see each other's data. Live-verified end to end over a real running server: deleted the source chunk between two identical live HTTP requests and confirmed the second request still returned the original, now-stale chunk data.
- [x] 135. Add benchmark dataset + regression-test script for retrieval — `eval/regression.py`, reusing `eval/fixtures.py`'s existing benchmark corpus (step 128) rather than building a second, near-identical one — it's already a real, labeled, deliberately well-separated dataset, and duplicating it would be pure duplication with no real additional value. A genuine SCRIPT, not just another pytest assertion: runnable standalone (`python -m eval.regression`, verified live — prints a real per-case report and exits 0/1), and wrapped by `tests/test_retrieval_regression.py` so a future regression fails CI automatically too, both calling the identical `run_regression()` function rather than keeping two implementations in sync. `MIN_PRECISION`/`MIN_RECALL` are both `1.0`, not an arbitrary looser bar — the benchmark corpus was built so every query has exactly one unambiguous correct answer, so any real drop below `1.0` here means something genuinely broke, not noise a looser threshold would need to absorb. Scoped to keyword search only, the one mechanism that works for real in every environment, same reasoning as every other honest environment-gap decision this session — a fake embedding provider would only prove fake-vector arithmetic still works, not catch a real dense-retrieval quality regression, so asserting a threshold there would be dishonest rather than a genuine guard.
- [x] 136. Document knowledge pipeline + RAG architecture in `docs/ARCHITECTURE.md` — fills in the "Knowledge pipeline & RAG" section (previously a placeholder), covering the full ingestion (82-117) and retrieval (118-135) pipeline as actually built, grounded in each step's own real design decisions and documented honest limitations rather than aspirational. Also updates the Status line (Milestone 3 complete, Milestone 4 next). Along the way, noticed the "Authentication & authorization" section was still a stale placeholder despite Milestone 2 having shipped long ago — out of this step's own scope (a different milestone's documentation debt), so flagged as a separate background task rather than silently bundled in or silently ignored. **This closes Milestone 3 — Knowledge Pipeline: Ingestion + RAG (steps 82-136) — in full.**

## Milestone 4 — Agent System (steps 137–161)

- [x] 137. Add LangGraph dependency + base agent-graph scaffold — first step of Milestone 4. `agent_graph.py`: a deliberate bare scaffold, not real orchestration logic — same "minimal, honest placeholder now, real logic added by later, real steps" shape `agents/base.py`'s own original `Agent` class had before real agents used it. The installed `langgraph` version (1.2.11) is a major-version jump past any assumption about its API shape, so nothing here trusted memory — its real `StateGraph`/`add_node`/`add_edge`/`compile`/`ainvoke` API was verified live via a throwaway probe script (built a real minimal graph, ran it end to end) before writing anything. `GraphState` is a minimal, generic input/output `TypedDict`, not yet this project's own Agent-shaped node contract (step 138's job). `build_base_graph()`'s one node is an honest identity passthrough — its only job is proving the real plumbing works end to end in this codebase's real async runtime, nothing more. `CompiledStateGraph` needed explicit generic type arguments (`[GraphState, None, GraphState, GraphState]`) to satisfy mypy strict — a real error caught and fixed, not guessed at. Tested for real: the compiled graph actually runs the passthrough node end to end, and `build_base_graph()` returns an independent graph on each call rather than a shared singleton. No new HTTP endpoint — no live-server verification needed, pure library-level scaffold.
- [x] 138. Add `Agent` base class/interface (input/output schema, config) — `agents/base.py:Agent` gains a real, generic `run(input) -> output` contract and a `config` dict, using PEP 695 type parameter syntax (matching `repositories/base.py:TenantScopedRepository`'s own precedent). LangGraph (137) is the concrete reason a uniform invocation contract is finally needed — the original step-097 `Agent` (just `name: str`) had already reasoned explicitly about why forcing a shared method would have traded real clarity for uniformity nobody needed at the time; that calculus changes now that a graph node needs SOME generic way to invoke whatever agent it wraps. `run()` is deliberately NOT an `abstractmethod` — making it one would break every existing subclass, none of which implement it and none of which are retrofitted here. The three existing concrete agents (`DocumentAnalysisAgent`, `ChunkingRecommendationAgent`, `RetrieverAgent`) keep their own domain-specific methods (`.analyze()`/`.recommend()`/`search_*()`) as their real, primary API — parameterized `Agent[Any, Any]` since they don't participate in the new contract; a future step (143, wiring Retriever Agent into the orchestrator graph) wraps them for graph use only once something real needs to, the same "wrap existing logic behind a new interface, don't rewrite it" pattern step 124 itself used. `agents/__init__.py`'s own docstring updated to match (Registry/Orchestrator still don't exist — that's 139/140). Tested for real: default/provided `config`, `run()` raising `NotImplementedError` when not overridden, and a real subclass overriding `run()` working end to end — plus the full existing test suite for all three affected concrete agents confirmed as zero regression.
- [x] 139. Add Agent Registry (register/discover/health-check) — `agents/registry.py:AgentRegistry`, the real machinery `agents/__init__.py`'s own docstring has been deferring since step 095 ("build that machinery when something actually needs to choose between agents at runtime, not before"). Starts with nothing pre-registered — none of the three existing concrete agents implement `run()` (step 138), so registering them would be dishonest machinery pretending to more capability than exists; real registrations start once a real `run()`-implementing agent exists (144+). `register()`/health are deliberately separate operations, matching how a real service registry lets something register before it's ready and reports readiness separately rather than refusing entry outright. `health_check()` is narrow and honest about what's actually checkable today (no LLM/network-dependent agent exists in this registry yet) — reports whether each agent's own `run()` was actually overridden (`type(agent).run is not Agent.run`, verified live before trusting it) rather than left as the base class's `NotImplementedError` stub, a real signal for "would invoking this agent right now have any chance of working," not a genuine external liveness probe dressed up as one. Duplicate registration and lookup of an unregistered name both raise (`AgentAlreadyRegisteredError`/`AgentNotFoundError`) rather than silently overwriting/returning `None`, same "fail loud on a caller bug" precedent used throughout this codebase. `agent_registry` is a module-level singleton for real app-wide use; tests construct their own fresh instances to avoid cross-test pollution. Tested with real fake agents (one with a real `run()` override, one without): register/get round-trip, duplicate-registration and unregistered-lookup both raising, `discover()` sorted and correct on both populated and empty registries, `health_check()` correctly distinguishing the two agents, and a healthy registered agent's `run()` actually working end to end.
- [x] 140. Add Orchestrator service skeleton — `orchestrator.py:Orchestrator`, a real service holding a real `AgentRegistry` (139) dependency and a real, compiled LangGraph `StateGraph`, built fresh rather than reusing `agent_graph.py`'s own throwaway scaffold (that module's own job was proving LangGraph works at all, step 137, already done). `OrchestratorState` carries only `query`/`response` today — adding `intent`/`plan`/agent-result fields now, before steps 141 (intent analysis) and 142 (Planning Agent) exist to define their own real shape, would be exactly the kind of speculative guess this project's own "add the field when the step that needs it lands" discipline argues against. The one node (`_echo_node`) is an honest placeholder, not real orchestration — its only job is proving `Orchestrator.handle()` → a real graph → `ainvoke()` → a real response works end to end as a genuine service, the same "prove the plumbing, not fake the logic" reasoning `agent_graph.py`'s own passthrough node used. Real mypy-strict catch: LangGraph's `ainvoke()` is typed `dict[str, Any] | Any` (verified live) — an explicit `cast()` back to `OrchestratorState` was needed so `Any` doesn't leak into `handle()`'s own real `str` return type; a bare assignment annotation alone wasn't enough (mypy still flagged the assignment itself as incompatible). Tested for real: `handle()` runs the real graph end to end, the orchestrator holds the exact registry instance it was constructed with, and two separate `Orchestrator` instances build fully independent compiled graphs rather than sharing one. No new HTTP endpoint — no live-server verification needed, pure service-level skeleton.
- [x] 141. Add intent-analysis step in orchestrator — `_intent_analysis_node`, the first real node added to `orchestrator.py`'s graph. Scoped tightly to what this codebase can actually act on today: AGENTS.md's own "INTENT ANALYSIS" section lists ten example categories (question answering, document search, conversation continuation, voice interaction, workflow execution, memory retrieval, knowledge update, document upload, analytics request, administrative task), but only "document search" has a real subsystem behind it right now (`RetrieverAgent`, 120-134) — conversation/memory/voice/workflows/analytics/admin are all later, unbuilt milestones. Building a ten-way classifier today, with nine categories nothing could route to, would be dishonest scaffolding pretending to more capability than exists. `document_search` vs. `empty` is the one real, checkable distinction available: a genuinely blank/whitespace-only query is never actionable regardless of what gets built later; everything else is — today — routed the one real way this system knows how to handle a query. `intent` stays internal graph state, not part of `handle()`'s own external `str` contract — nothing outside the graph consumes it yet; step 142's Planning Agent is the real, first consumer. Verified live before trusting it: LangGraph tolerates a partially-populated initial `ainvoke()` dict at runtime (a later node's own output still merges correctly even when an earlier field is omitted from the caller's input), but mypy strict still requires the full state shape on the call site — `handle()`'s own `ainvoke()` call needed `intent` added explicitly to satisfy the type checker even though the runtime behavior didn't require it. Tested for real: `_classify_intent` on a genuine query/empty-string/whitespace-only query, and the compiled graph itself correctly setting `intent` end to end for both a real query and a blank one.
- [x] 142. Add Planning Agent (selects agents + execution order) — `agents/planning.py:PlanningAgent`, the first real `agents/base.py:Agent.run()` implementation in this codebase (step 138's own contract — `DocumentAnalysisAgent`/`ChunkingRecommendationAgent`/`RetrieverAgent` all stayed `Agent[Any, Any]` since none of them needed graph invocation for their own real callers; `PlanningAgent` genuinely does). `Plan` is just `agent_names: list[str]`, either `["retriever"]` or `[]` — `document_search` is still the only real intent with any agent behind it (step 141), so AGENTS.md's own other planning determinations (sequential vs. parallel execution, whether clarification is needed, confidence measurement) would all be meaningless with only ever zero or one real candidate agent in play; adding those fields now would be exactly the kind of speculative scaffolding this project has repeatedly rejected. Wires a real `_planning_node` into `orchestrator.py`'s graph (`intent_analysis` → `planning` → `echo`), storing `Plan.agent_names` into a new, justified `agent_names` state field. `"retriever"` is a plain string naming the agent this plan wants, not a lookup against `agents/registry.py:AgentRegistry` — nothing real is registered there yet (step 143's own job); deciding what SHOULD run and being able to actually EXECUTE it stay deliberately separate, sequential concerns — `_echo_node` doesn't read `agent_names` yet. Tested for real: the agent's own `run()` for both real intents and an unrecognized one (plans nothing rather than guessing), plus the full graph correctly setting `agent_names` end to end for both a real query and a blank one.
- [x] 143. Wire Retriever Agent into orchestrator graph — `_RetrieverGraphAgent`, the thin `Agent` adapter wrapping `agents/retriever.py:RetrieverAgent` for graph use that `agents/base.py`'s own step-138 docstring already named as this step's job. Deliberately NOT registered into `agents/registry.py:AgentRegistry` — that registry (139) assumes stateless, construct-once, look-up-many agents, but this adapter is genuinely request-scoped (`tenant_id`/`knowledge_base_id` differ per call), which its `register()` design never accounted for; forcing a per-request object into an app-wide singleton registry would be a worse fit than constructing it fresh inside the node that needs it, the same "no fixed caller yet, own its own session" shape `vectorstore/pgvector.py:PgVectorStore.search()` already established at step 119 — `Orchestrator._registry` stays genuinely unused by this step too, real infrastructure waiting for a stateless agent (144+) to actually need it. `OrchestratorState` gains `tenant_id`/`knowledge_base_id` (a real, justified addition — retrieval genuinely needs them); `Orchestrator.handle()` grows matching keyword parameters, the first honest shape a real caller will need since none exists yet to force a different one. Uses `ChunkRepository.search_by_keyword` specifically — the one mechanism that works with no `OPENAI_API_KEY` — and the response for a real hit is the retrieved chunks' own raw text, not a synthesized answer, since no chat/generation model exists yet to honestly produce one. Tested against real Postgres (`test_orchestrator.py`, extended): a seeded-but-empty knowledge base returns a real "No results found." message (verified live first that a genuinely unseeded tenant/kb pair also just returns zero rows rather than erroring, so the seeded-org version here is for realism, not to avoid a crash), a seeded chunk's real text comes back for a matching query end to end through `handle()`, and results are correctly scoped to the given knowledge base rather than leaking from a different one.
- [x] 144. Add Memory Agent skeleton — `agents/memory.py:MemoryAgent`, a genuine skeleton, the same shape `agents/document_analysis.py`/`agents/chunking_recommendation.py` originally had (095/097): a real class with a real `name`, no real logic — no `Memory` model or migration exists anywhere in this codebase (that's Milestone 5, steps 162-175), so there's nothing real for `run()` to call into. `run()` deliberately left unimplemented (`agents/base.py`'s own `NotImplementedError` default), not a stub silently returning empty/`None` — a stub would dishonestly imply "memory was checked, nothing found" when the true state is "memory doesn't exist as a concept in this codebase yet." `Agent[Any, Any]`, matching `DocumentAnalysisAgent`/`ChunkingRecommendationAgent`/`RetrieverAgent`'s own precedent for not participating in the typed `run()` contract. Proves the real, intended payoff of `agents/registry.py:AgentRegistry.health_check()`'s own design (step 139): registering this agent makes it discoverable (`registry.discover() == ["memory"]`) while `health_check()` honestly reports it unhealthy (`{"memory": False}`) until Milestone 5 gives it a real implementation — registration and health staying deliberately separate operations, exactly as designed. Tested for real: the agent's own shape, that `run()` genuinely wasn't overridden, that calling it raises `NotImplementedError`, and the registry correctly reporting it as discoverable-but-unhealthy.
- [x] 145. Add Conversation Agent skeleton — `agents/conversation.py:ConversationAgent`, same genuine-skeleton shape `agents/memory.py` established at step 144: real class, real `name`, no real logic — no conversation/session model or migration exists anywhere in this codebase yet (that's Milestone 6, steps 176-200). `run()` left unimplemented rather than stubbed, `Agent[Any, Any]`, same reasoning and same 4-test shape (base shape, `run()` genuinely not overridden, calling it raises, registry reports discoverable-but-unhealthy).
- [x] 146. Add Reasoning Agent skeleton — `agents/reasoning.py:ReasoningAgent`, same genuine-skeleton shape steps 144/145 established: real class, real `name`, no real logic — multi-step reasoning over complex questions needs a real chat/generation model, and none exists anywhere in this codebase yet (that's steps 150+). `run()` left unimplemented rather than stubbed, `Agent[Any, Any]`, same reasoning and same 4-test shape.
- [x] 147. Add Quality Review Agent skeleton — `agents/quality_review.py:QualityReviewAgent`, same genuine-skeleton shape steps 144-146 established: real class, real `name`, no real logic — reviewing a generated response's accuracy/completeness/hallucinations needs a real response to review in the first place, and no chat/generation model exists anywhere in this codebase yet (that's steps 150+). `run()` left unimplemented rather than stubbed, `Agent[Any, Any]`, same reasoning and same 4-test shape.
- [x] 148. Add Safety Agent skeleton (prompt-injection/unsafe-content checks) — `agents/safety.py:SafetyAgent`, same genuine-skeleton shape steps 144-147 established: real class, real `name`, no real logic — detecting prompt injection/malicious instructions/unsafe tool usage needs a real chat/generation and tool-execution pipeline to guard, and neither exists anywhere in this codebase yet (that's steps 150+). Cross-tenant protection specifically stays owned by existing, real, already-tested infrastructure (Postgres RLS + `TenantScopedRepository`) — this agent doesn't duplicate it, a future real implementation would compose with it. `run()` left unimplemented rather than stubbed to return a fake "safe" verdict — a no-op-but-confident-looking result here would be actively dangerous, not just dishonest, so `NotImplementedError` is the honest, safer default. `Agent[Any, Any]`, same reasoning and same 4-test shape as every prior skeleton.
- [x] 149. Add Citation Agent (assembles citations into response) — `agents/citation.py:CitationAgent`, breaking the skeleton streak (144-148): `citations.py:build_citations()` already exists as real, working, tested logic (step 127), so this is a thin adapter wrapping it, the same "wrap existing real logic, don't rewrite it" pattern step 143 used for `_RetrieverGraphAgent`. `build_citations(chunks, *, document_info)` genuinely needs two inputs that both vary per call (unlike step 143's `RetrieverAgent`, where `tenant_id`/`knowledge_base_id` were legitimately stable, constructor-injectable config) — no honest "stable config vs. varying input" split exists here, so both are bundled into one `CitationAgentInput` rather than forcing a fake one. Genuinely stateless (no constructor args at all, `citations.py`'s own logic being a pure function) — the first real candidate for `agents/registry.py:AgentRegistry`'s (139) actual registration use case, though still not auto-registered anywhere (nothing in this codebase constructs-and-registers real agents at app startup yet). Tested for real: the agent's own shape, that `run()` is genuinely overridden (unlike every prior skeleton), delegating correctly to real `build_citations()` (including real section extraction), empty input, and — the real payoff of step 139's own `health_check()` design — registering this agent reports it as healthy (`True`), the first agent in this codebase to do so instead of `False`.
- [x] 150. Add LLM-provider abstraction interface — `llm/base.py:LLMProvider`, interface-first, same precedent `embeddings/base.py:EmbeddingProvider` (106) and `vectorstore/base.py:VectorStore` (118) already established: the roadmap explicitly sequences this abstraction before either concrete implementation (151 OpenAI, 152 Anthropic). Message-based (`list[Message]`, each a role/content turn), not a single prompt string — every real provider this roadmap will actually implement is a chat/messages API, and this codebase's own Conversation Agent/Conversation Engine (Milestone 6) already commit to multi-turn conversation being the real shape a caller needs. `Role` includes `"system"` even though Anthropic's real Messages API takes system instructions as a separate top-level parameter, not a role inside the messages list — a real, known provider quirk left for step 152's own adapter to translate, keeping this shared interface the portable, common shape rather than bending to one provider. `LLMResponse` carries real `prompt_tokens`/`completion_tokens` — step 153 ("execution tracing: latency/tokens/status") is already known to need real token counts, and only a provider's own API response can honestly report them; latency is deliberately NOT part of the interface, since any caller can already measure it externally via wall-clock time. No streaming method and no `PROVIDERS` registry yet — neither is asked for by this roadmap's own sequencing (150-161) today; the registry will genuinely earn its keep once step 152 lands a real second implementation, matching `auth/oauth.py`'s own "registry appears once a second real provider exists" precedent. `LLMProviderError` mirrors `embeddings/openai.py:EmbeddingProviderError` — one type for any real provider failure. Tested with a real fake implementation (`_FakeLLMProvider`, not a mock), same reasoning `test_embedding_provider.py`/`test_vectorstore_base.py` already established: structural Protocol satisfaction, a real multi-turn conversation producing a real response. Pure interface, no HTTP/DB touch — no live-server verification needed, same as step 106's own precedent.
- [x] 151. Implement OpenAI LLM provider — `llm/openai.py:OpenAIProvider`, the first real `LLMProvider` (150), same "direct httpx, no vendor SDK" reasoning `embeddings/openai.py:OpenAIEmbeddingProvider` already established. Speaks Chat Completions (`/v1/chat/completions`), not the newer Responses API — checked live against OpenAI's own current docs before choosing: Chat Completions is explicitly "supported indefinitely" even though Responses is OpenAI's own recommended surface for brand-new integrations; Chat Completions' exact shape is what `llm/base.py:Message`/`LLMResponse` were already designed to mirror, and it's one of the most stable, longest-unchanged request/response shapes in the industry — the safer, higher-confidence choice over an unfamiliar newer API. `gpt-5.6-luna` chosen as the default model: OpenAI's own current cheapest chat tier (checked live), the same "cheapest current model, not a speculative mid-tier compromise" reasoning `embeddings/openai.py` already used for `text-embedding-3-small` — no real caller commits this codebase to a specific quality/cost tradeoff yet (every would-be caller, 144-148, is still an honestly-unimplemented skeleton). `Message.role` needs no translation for this provider — Chat Completions natively accepts `"system"` as a normal message role, unlike a future Anthropic adapter. **Real live verification beyond the standard `httpx.MockTransport` tests:** ran a throwaway probe script against the actual `api.openai.com` endpoint and found a genuinely more precise failure mode than assumed — with this environment's empty API key, `f"Bearer {settings.openai_api_key}"` becomes `"Bearer "` with a trailing space, which httpx's own client-side header validation rejects as `httpx.LocalProtocolError` *before* the request ever reaches the network, not a server-returned 401 as first expected; confirmed live that `LocalProtocolError` is a real subclass of `httpx.HTTPError`, so the existing `except (httpx.HTTPError, ...)` clause already catches it correctly — the observable behavior (a clean, generic `LLMProviderError`) is unaffected, this is a more precise understanding of the exact mechanism, not a bug. Tested with `httpx.MockTransport` (same technique `test_openai_embedding_provider.py` established at step 107) against a realistic Chat Completions response shape: real request body verified (model, messages), successful parsing, and both an HTTP failure and a malformed response correctly raising `LLMProviderError`.
- [x] 152. Implement Anthropic LLM provider — `llm/anthropic.py:AnthropicProvider`, the second real `LLMProvider` (150), following `llm/openai.py`'s own "direct httpx, no vendor SDK" shape, verified live against Anthropic's own current docs (platform.claude.com/docs/en/api/messages — docs.claude.com redirects there) before writing any code, same discipline used for LangGraph (137) and OpenAI (151). Three real, confirmed-live shape differences from OpenAI, each handled explicitly rather than reused: (1) `system` is a top-level request parameter, not a message role — `llm/base.py`'s own step-150 docstring already flagged this exact translation as this step's job; `_split_system` pulls any `role="system"` messages out of the input list and sends the remainder as `messages`. (2) `max_tokens` is a *required* field (unlike OpenAI's server-side default) — set to a fixed, generous default since no real caller commits this codebase to a length budget yet, same "no real caller yet" reasoning `llm/openai.py`'s own model choice used. (3) Auth is `x-api-key` + a required `anthropic-version` header, not a Bearer `Authorization` header. Response `content` is an array of typed blocks (text/tool_use/thinking/...), not a single string — takes the first `type: "text"` block and raises `LLMProviderError` if none exists rather than assuming `content[0]` is text; `usage.input_tokens`/`usage.output_tokens` map to `LLMResponse.prompt_tokens`/`completion_tokens`. Model: `claude-haiku-4-5`, Anthropic's own current fastest/cheapest tier (checked live), same cost-conscious default reasoning as 151. **Real live verification beyond `httpx.MockTransport`:** ran a throwaway probe against the actual `api.anthropic.com` endpoint and found a genuinely different failure mode than step 151's OpenAI probe — this environment's empty `anthropic_api_key` produces a clean, server-returned `401 Unauthorized`, NOT the client-side `httpx.LocalProtocolError` OpenAI's `"Bearer "` (trailing space) triggered, because Anthropic's `x-api-key` header takes the raw key with no prefix, so an empty value is still a syntactically valid header httpx has no reason to reject client-side. Same observable behavior either way (a clean `LLMProviderError`), but confirms the two providers' auth conventions genuinely differ enough to fail at different layers for the same "empty key" input — worth knowing rather than assuming step 151's finding would just repeat. `PROVIDERS: dict[str, LLMProvider]` also added to `llm/__init__.py` at this step (as that file's own step-150 docstring already committed to), mirroring `auth/oauth.py`'s registry shape exactly (name → real instance) — no router/agent is keyed on provider name yet, so this is tested infrastructure ready for that caller, same as `AgentRegistry` starting empty-but-real at step 139. Tested with `httpx.MockTransport`: system-message splitting into the top-level parameter, omitting `system` entirely when absent, picking the first text block out of a mixed content array, `LLMProviderError` on no-text-block/HTTP-failure/malformed-response, plus a `PROVIDERS` registry test confirming both real providers are present under their names.
- [x] 153. Add per-agent execution tracing (latency/tokens/status) — `agents/tracing.py:traced_run`, scoped tightly to exactly the roadmap's own parenthetical (latency/tokens/status), not AGENTS.md's full aspirational "AGENT OBSERVABILITY" field list (retry count, confidence, resource usage, future cost estimation, ...) — none of those have a real caller or meaning in this codebase yet, so adding them now would be machinery with nothing real behind it, the same discipline `llm/__init__.py`'s PROVIDERS registry (152) and `agents/registry.py` (139) already followed. Generalizes `agents/retriever.py`'s own per-call structured-logging pattern (step 129, `_log_retrieval`) from retrieval specifically to any `Agent.run()` call: `traced_run(agent, input)` times the call, logs one `"agent_execution"` event (`agent_name`/`status`/`latency_ms`/`prompt_tokens`/`completion_tokens`) via `logging_config.py`'s existing `get_logger`, and re-raises failures unchanged after logging a `status="failure"` trace — a deliberate broad `except Exception` that observes without altering control flow, unlike `llm/openai.py`'s narrow provider-specific exception mapping. Wired into `orchestrator.py`'s two real per-agent call sites (`_planning_node`, `_execute_node`) — the only real per-agent invocations this codebase's request flow makes today, matching AGENTS.md's own "END-TO-END EXECUTION TRACING" diagram (Planning Agent, Selected Agents). Tokens are real, not faked: `prompt_tokens`/`completion_tokens` stay `None` unless the agent's output is genuinely an `llm.base.LLMResponse` (150) — no agent's `run()` calls an LLM yet (144-148 are still honest skeletons; 142/149 don't call one either), so every trace this step can currently produce reports no tokens, an honest gap rather than a placeholder. Tested with `structlog.testing.capture_logs()` (same technique `test_retriever_agent.py` established at 129): real output passthrough, a success event with no tokens, a failure event that still re-raises the original exception, and a real token count reported for a fake LLM-backed agent.
- [x] 154. Add parallel execution for independent agent steps — `agents/parallel.py:run_parallel`, real generic infrastructure ("Determine where agents can safely execute in parallel," AGENTS.md's own "PARALLEL EXECUTION" section) shipped ahead of its first production caller — same precedent `agents/registry.py:AgentRegistry` (139) and `llm/__init__.py:PROVIDERS` (152) already established. `orchestrator.py` is deliberately NOT changed by this step: `agent_names` can only ever be `["retriever"]` or `[]` today (`agents/planning.py:PlanningAgent` has exactly one real branch), so there's no genuine multi-independent-agent request in production traffic yet — wiring a single-item list through it would be a no-op, and manufacturing a second branch with a still-skeleton agent (e.g. `MemoryAgent`, still `NotImplementedError`) would break every real document-search request today; neither is honest. Runs each `(agent, input)` pair through `agents/tracing.py:traced_run` (153), not a bare `.run()`, so tracing keeps reporting per-agent status/latency/tokens regardless of sequential vs. concurrent execution. Uses `asyncio.gather`'s default (not `return_exceptions=True`) — a failure in any step fails the whole batch, since partial-result degradation is explicitly step 155's job, not this one's. Deliberately infers no dependency graph — AGENTS.md's own "without increasing inconsistency" warning means "is this safe to parallelize" stays the caller's decision, same "caller supplies the correctly-scoped input" discipline `search_multi_query` (130) already established; `agents/citation.py:CitationAgent` is this codebase's one concrete example of an agent that's NOT a valid candidate (it needs a prior retrieval's own output as input). Tested with real independent fake agents: results returned in order, each step traced independently, a failure from any one step propagating correctly, and — the real payoff — a wall-clock timing assertion (three 0.2s-delayed agents completing in well under 0.6s) proving genuine `asyncio.gather` concurrency rather than a sequential loop that would happen to produce the same return value.
- [x] 155. Add agent failure handling (retry, fallback, partial-result degradation) — scoped tightly to exactly the roadmap step's own parenthetical, not AGENTS.md's fuller "FAILURE HANDLING" list (alternative strategy, human-readable errors, confidence reduction, escalation) — none of those have real meaning here yet (no agent reports a confidence score at all, per `agents/tracing.py`'s own step-153 scoping; "escalation" implies a human-in-the-loop workflow nothing here builds). `agents/resilience.py:with_retry` retries a failing agent call with linear backoff, letting the real final failure propagate unwrapped rather than raising a generic error; `with_fallback` tries a primary agent and falls back to a second on any failure. Both wrap `agents/tracing.py:traced_run` (153), not a bare `.run()`, so every attempt — including retried/replaced ones — gets its own real tracing event. No retry library (tenacity/backoff) added as a dependency: this project's only existing retry precedent is Celery's own `autoretry_for`/`retry_backoff` (`embeddings_pipeline.py`, step 112), which exists only for Celery *tasks* — agent execution runs inline inside a request/response cycle with no Celery task wrapping it, so a small explicit async loop is the honest equivalent, same "raw approach over a library that buys nothing extra" reasoning `llm/openai.py`/`embeddings/openai.py` already used for HTTP calls. `agents/parallel.py:gather_partial` adds the third item — the partial-result-degradation `run_parallel`'s own step-154 docstring explicitly deferred here: returns one `AgentStepResult` (`ok`/`failed`) per step instead of failing the whole batch on any single failure, a distinct function rather than a flag since the two have genuinely different return shapes. Tested for real: `with_retry` succeeding after real transient failures (a flaky fake agent with a mutable call counter, not a mock), raising the real underlying error after exhausting attempts, making exactly one call when `max_attempts=1`; `with_fallback` using the primary's result on success, the fallback's result on primary failure, and re-raising when both fail; `gather_partial` returning a mixed ok/failed batch in order and an all-ok batch when nothing fails.
- [x] 156. Add per-agent unit tests (mocked providers) — audited every agent for a genuine "mocked providers" gap rather than assuming one existed: every skeleton agent (144-148), `CitationAgent` (149), `PlanningAgent` (142), and `agents/tracing.py`/`parallel.py`/`resilience.py` (153-155) already had real, isolated tests with no provider dependency to mock. `RetrieverAgent` is the one agent that depends on a provider-shaped collaborator with no fakeable Protocol seam: `repositories/chunk.py:ChunkRepository` is a concrete SQLAlchemy-backed class, unlike `EmbeddingProvider`/`VectorStore` (both structural Protocols, already given real fakes in `test_retriever_agent.py` since step 124) — `unittest.mock.AsyncMock(spec=ChunkRepository)` is the honest, deliberate exception to this project's usual "real fake over a mock" preference, precisely because there's no interface to fake against. New file `tests/test_retriever_agent_mocked_repository.py`, additive to (not a replacement for) `test_retriever_agent.py`'s existing real-Postgres tests — that file's own docstring already gives a considered reason to keep testing `ChunkRepository.search_by_keyword`'s real full-text-search correctness against a real tsvector column ("no mocks for infrastructure this project owns"); what these new tests isolate instead is `RetrieverAgent`'s own glue logic (result mapping + structured logging in `search_keyword`, fusion in `search_hybrid`, pure delegation in `expand_to_parent`) independent of Postgres's real ranking behavior — AGENTS.md's own "Unit tests" and "Integration tests" as two separate, complementary things, not one substituting for the other. `expand_to_parent` is the clearest case: it has no logic of its own beyond `return await chunk_repo.get_expanded_context(...)`, so asserting the exact mocked call made (`assert_awaited_once_with`) IS the right-level test — `test_retriever_agent.py`'s own existing DB-backed test for it already said as much in its own docstring before this step. Duplicated the small `_FakeEmbeddingProvider`/`_FakeVectorStore` fakes locally rather than importing them from `test_retriever_agent.py`, matching this project's established convention of only cross-importing test helpers from `tests/helpers.py`, never test-module-to-test-module.
- [x] 157. Add orchestrator integration test (full request→response trace) — audited `test_orchestrator.py` first (same audit-before-building discipline step 156 established): it already had substantial real, DB-backed integration coverage of `handle()`'s RESPONSE value (no-results, real-hit, KB-scoping, empty-query short-circuit), but nothing verified the request was actually *traced* end to end. Added two new tests using `structlog.testing.capture_logs()` around a real `handle()` call: `test_handle_produces_a_full_trace_across_every_real_agent_step` asserts a real document-search request traces exactly `["planning", "retriever"]` in order, both `status="success"`, alongside the correct real response; `test_handle_traces_planning_but_not_retriever_for_an_empty_query` asserts an empty query traces only `["planning"]` — `_execute_node`'s echo short-circuit means the retriever agent is genuinely never constructed or called, and the trace honestly reflects that rather than claiming a step ran that didn't. This is the integration-level guarantee neither step 153 (tracing, tested via fake agents in isolation) nor steps 140-143 (the orchestrator, tested for response values only) proved on its own — the real payoff of wiring `traced_run` into the real graph nodes rather than just unit-testing the wrapper.
- [x] 158. Add per-assistant agent-configuration model — `agents/configuration.py:AgentConfiguration`, scoped to just the "Agent configuration" bullet from AGENTS.md's own "AI ASSISTANTS" section list (Name/Description/Instructions/Knowledge access/Voice configuration/Memory settings/Security policies/Deployment settings/Analytics/Future tool integrations are each a separate, later concern). Built ahead of the `Assistant` DB model (step 159, doesn't exist yet) as a standalone, validated Pydantic model — most likely to become a JSONB column there, matching `models/document.py`'s own `doc_metadata` JSONB pattern for genuinely multi-field, flexible per-record data; Pydantic (not a plain dataclass, unlike most of this codebase's other `agents/` data shapes) is the right base specifically because real field validation is the point here. `llm_provider` validates against `llm.PROVIDERS`' real, live keys (152) rather than a hardcoded `Literal` — can't silently drift when a third provider is added. `enabled_agents` validates against `KNOWN_AGENT_NAMES`, sourced directly from each real, request-time agent's own `.name` class attribute (retriever/citation/planning/memory/conversation/reasoning/quality_review/safety), deliberately excluding `document_analysis`/`chunking_recommendation` — both real agents, but ingestion-time, never part of an assistant's own runtime request path; naming an agent here doesn't promise it does anything yet (five of the eight, 144-148, are still honest skeletons) — this model validates the name is real and recognized, not that the agent behind it is implemented. `retrieval_top_k` reuses `RetrieverAgent`'s own real `top_k` parameter, bounded `ge=1, le=50`. Lives in `agents/`, not `schemas/` — it needs to import real agent classes, and `schemas/` must never import from `agents/` (this codebase's own layering rule, applied here in its normal direction). Tested for real: defaults, accepting the second real provider, rejecting an unregistered provider/unrecognized agent name/out-of-bounds `top_k`, `KNOWN_AGENT_NAMES` correctly excluding the two ingestion-time agents, and a dict round-trip proving JSON-serializability for the future JSONB column.
- [x] 159. Create `Assistant` model + migration — `models/assistant.py:Assistant`, sits under `KnowledgeBase` in the product hierarchy (AGENTS.md's "ORGANIZATION STRUCTURE": Organization → Workspace(s) → Knowledge Base(s) → AI Assistant(s) → ...), mirroring `KnowledgeBase`'s own one-parent-FK shape under `Workspace` (082). Genuinely model-only — same precedent `Document` (083) and `Chunk` (105) already set — since step 160 ("Add Assistant CRUD endpoints") is its own separate roadmap step, unlike `KnowledgeBase`, which bundled minimal CRUD into its own model step because no dedicated later CRUD step existed for it; no repository yet either. Fields deliberately minimal beyond `name`/`slug`/`description` (same "don't speculate about fields a real step hasn't asked for" discipline `KnowledgeBase`'s own docstring applied) — AGENTS.md's "AI ASSISTANTS" section lists many more per-assistant concerns (instructions, knowledge access, voice configuration, memory settings, security policies, deployment settings, analytics, future tool integrations), but only `agent_configuration` has a real, already-built shape to store: step 158's `agents/configuration.py:AgentConfiguration`, built specifically for this. Stored as JSONB (`Mapped[dict[str, object]]`), matching `models/document.py`'s own `doc_metadata` JSONB pattern — validated structure at the application layer (the future CRUD endpoint's job), flexible storage at the DB layer; the model itself stays SQLAlchemy-only, no Pydantic import, matching every other `models/*.py` file. `slug` unique within `knowledge_base_id`, not tenant-wide, same reasoning `KnowledgeBase`'s own slug constraint used one level up. Migration generated via real `alembic revision --autogenerate` against the live DB, RLS calls added manually matching every prior tenant-scoped table's migration shape, and RLS confirmed genuinely enabled live against Postgres (`relrowsecurity = t`), not just assumed from calling `enable_rls()`. Tested directly at the ORM/RLS layer (no router to go through yet): create/read including the real JSONB default (`{}`), a real `AgentConfiguration` round-tripping through the JSONB column via `model_dump()`/`model_validate()`, duplicate-slug-within-KB rejection, and cascade-delete when the parent knowledge base is deleted.
- [x] 160. Add Assistant CRUD endpoints — `routers/assistant.py`: create/list/get/delete, nested three levels under organization (`/organizations/{id}/workspaces/{id}/knowledge-bases/{id}/assistants`), matching the product hierarchy, one level deeper than `routers/knowledge_base.py`'s own nesting. No update endpoint — same scope as `knowledge_base`/`workspace`, both of which don't have one either; "CRUD" in this codebase's own usage has consistently meant this four-operation set. Reuses `dependencies/knowledge_base.py:TargetKnowledgeBase` directly rather than duplicating a resolution dependency — the exact "resolve + cross-check the URL's knowledge_base_id against a real row in this tenant/workspace" pattern `routers/document.py`/`routers/retrieval.py` already share. `schemas/assistant.py`'s `agent_configuration` field is a real `agents/configuration.py:AgentConfiguration` (158), not a bare dict — validated at the API boundary before it ever reaches the JSONB column, the concrete payoff of building that model ahead of time. New `assistant:create/read/delete` permissions seeded via migration, granted to the same `org_owner`/`admin`/`manager` tier as `knowledge_base`'s own permissions — no reason for an assistant's access tier to differ from the resource one level up. **Real bug found and fixed along the way:** `errors.py`'s `RequestValidationError` handler passed `exc.errors()` straight into `JSONResponse`, but pydantic embeds the raw exception object in `ctx["error"]` for any `field_validator` that raises `ValueError` — `AgentConfiguration` is this codebase's first `field_validator`, and plain `json.dumps` can't serialize that, silently turning a real 422 into an unhandled 500. Fixed with `jsonable_encoder` (the same fallback FastAPI's own default handler already relies on), verified with a real invalid-`llm_provider` request now correctly returning 422. Also updated `test_rbac_enforcement.py`'s `EXPECTED_ROLE_PERMISSIONS` systematic matrix with the three new permissions (caught by that test's own real, live re-run — not something that could've been missed silently). Tested against the real FastAPI app (`TestClient`, no mocks): auth-required, non-member 403, full create/get/list/delete cycle including a real `agent_configuration` round-trip, defaulting when omitted, rejecting an invalid `llm_provider` with a real 422, `end_user` role 403, cross-knowledge-base 404 isolation, and duplicate-slug-within-KB conflict (but not across sibling KBs) — mirroring `test_knowledge_base_endpoints.py`'s own full test shape one nesting level deeper.
- [x] 161. Document multi-agent architecture in `docs/ARCHITECTURE.md` — filled in the previously-placeholder "Multi-agent system" section, matching the "Knowledge pipeline & RAG" section's own established style (subsections, honest tracked gaps at the end): Agents (the real/skeleton split, `Agent[InputT, OutputT]` base, `AgentRegistry`), Orchestrator (the real LangGraph three-node graph, `OrchestratorState`'s incremental growth, `_RetrieverGraphAgent`'s request-scoped adapter), LLM providers (OpenAI/Anthropic, `PROVIDERS` registry, no real caller yet), Execution infrastructure (tracing/parallel/resilience), and Assistants (the new `Assistant`/`AgentConfiguration` layer). Updated the Status line: Milestone 4 (Agent System) complete, Milestone 5 (Memory System) next. Documentation-only, no code changes — no lint/type-check/test/CI concern. **Milestone 4 (Agent System) complete — 161/300.**

## Milestone 5 — Memory System (steps 162–175)

- [x] 162. Create `Memory` model + migration (short-term/long-term/user/org/session) — `models/memory.py:Memory`, the long-term, Postgres-backed half of the memory system, scoped tightly to exactly the roadmap step's own parenthetical: `memory_type` ("short_term"/"long_term") and `scope` ("user"/"organization"/"session"), not AGENTS.md's fuller memory-type list (assistant/workspace/agent memory — no real caller yet, not part of this step's own literal wording). `memory_type` genuinely supports both values, but only "long_term" is ever written by real code today — step 163's own "Redis-backed short-term memory store" is an entirely separate, conversation-scoped Redis mechanism that never touches this table; the column exists honestly as the full real taxonomy, not a speculative guess, same "field exists, only some values currently reachable" precedent `Document.chunking_strategy_source` already established. `scope="organization"` needs no extra column since `tenant_id` (required on every row) already identifies the org; `scope="user"` populates `user_id` (real FK to `users.id`, CASCADE); `scope="session"` populates `session_id` — deliberately NOT a foreign key, since no `Conversation`/`ConversationSession` model exists yet (Milestone 6) — the same "field lands ahead of its full wiring, documented as a real gap" precedent `AuditLog.actor_user_id` already established before auth existed. No `importance_score` column — step 164's own parenthetical ("Postgres-backed long-term memory store (importance-scored)") makes that column's owner explicit; speculating about its shape now would be designing against a guess. RLS enabled and verified live against Postgres (`relrowsecurity = t`). Migration generated via real `alembic revision --autogenerate`. Genuinely model-only — no repository or Memory Agent logic yet, same precedent `Document`/`Chunk`/`Assistant` already set (steps 164-165 are their own separate jobs). **Real bug found and fixed while writing tests:** this is the first model-only test in this codebase to create a real `User` row directly via ORM (not through the signup endpoint) — without explicit cleanup, its fixed test email collided with `ix_users_email` on the very next run, since `users` is a global table (not tenant-scoped) and the org-slug cleanup every other model test here relies on never touches it; fixed with an explicit `_cleanup_user` helper, verified by running the test file twice in a row with no external cleanup between runs. Tested at the ORM/RLS layer: create/read for all three scopes (including the real `session_id` unconstrained-UUID case), the real `memory_type` default, and cascade-delete on both organization deletion and user deletion.
- [x] 163. Add Redis-backed short-term memory store (conversation-scoped) — `short_term_memory.py:append_turn`/`get_recent_turns`/`clear`, matching `rate_limit.py`'s own established "standalone module using `redis_client.py`'s shared client directly" pattern rather than a new client. A Redis LIST per conversation (`RPUSH`/`LTRIM`/`LRANGE`), not a JSON blob — "maintains active conversation context" (AGENTS.md's own "MEMORY REQUIREMENTS") means a bounded, ordered recent-turn window, which a list gives for free with per-command atomicity a read-modify-write blob cycle wouldn't have under concurrent writers. Entries are real `llm.base.Message` (role/content) — the exact shape a future chat call needs to reconstruct a prompt, not a parallel dataclass reinventing the same two fields. `session_id` is the same unconstrained identifier `models/memory.py:Memory.session_id` (162) already anticipates — both will reference the same real conversation once Milestone 6's Conversation model lands. Bounded to `MAX_ENTRIES` (50, `LTRIM` after every push), TTL refreshed on every write (`DEFAULT_TTL_SECONDS`, one hour) — genuinely short-term, an idle conversation's working memory expires on its own, unlike anything promoted into long-term storage (that promotion decision is step 165's own job). No environment-gating in this module itself, unlike `rate_limit.py`'s FastAPI-facing dependency wrapper — nothing here is wired into an HTTP request path yet, so there's no `TestClient`-driven suite to protect from Redis's known event-loop-binding-under-pytest issue. Tested directly against real Redis, same isolation `test_rate_limit.py` already established (explicit disconnect-after-test): round-trip ordering, an untouched session returning empty, session isolation, `MAX_ENTRIES` correctly trimming the oldest turns while preserving order, a real TTL, and `clear()`.
- [x] 164. Add Postgres-backed long-term memory store (importance-scored) — `models/memory.py:Memory` gains `importance_score` (plain `float`, app-level convention 0.0–1.0, no DB `CHECK` — same "convention, not a hard constraint" choice `scope`/`memory_type` already made — defaults to 0.5 neutral), the column step 162's own docstring explicitly deferred to this step. `repositories/memory.py:MemoryRepository` adds `list_for_user`/`list_for_organization`/`list_for_session` — one per real scope (162) — each ordered by `importance_score` descending then `created_at` descending, so the most important memories come back first, not insertion order; a `min_importance` filter (default 0.0, matches everything, so existing behavior is unchanged for a caller that doesn't pass it) lets a future caller exclude low-value memories entirely. Computing a real importance score is deliberately NOT this step's job — step 165's own "Memory Agent logic: decide what deserves long-term retention" owns that decision; this repository only stores and retrieves by whatever score a caller provides. Migration generated via real `alembic revision --autogenerate`, applied against the live DB. Tested: importance-ordering across three real scores, `min_importance` filtering, per-user and per-session isolation (including excluding organization-scoped rows from a user query), and the real default score when none is passed.
- [x] 165. Add Memory Agent logic: decide what deserves long-term retention — `agents/memory.py:MemoryAgent` graduates from the step-144 skeleton, the first Milestone-4-skeleton agent to gain real logic. AGENTS.md's own "MEMORY AGENT" section lists many more responsibilities (retrieval, updates, summarization, cleanup, confidence, expiration, quality) — scoped tightly to exactly this step's own wording ("decide what deserves long-term retention... not every conversation should become permanent memory"); each of the rest is its own later roadmap step (166 retrieval, 167 summarization, 168 expiration, ...). `run(Message) -> RetentionDecision` is a real, deterministic heuristic, not an LLM judgment — no agent in this codebase calls a chat/generation model yet, same discipline `PlanningAgent` already applied to its own decision: content shorter than 15 characters scores 0.1 (near-certain noise like "ok"/"thanks"), content containing an explicit identity/preference signal phrase (the kind AGENTS.md's own "PERSONALIZED EXPERIENCE" section names — name, email, preferences, "remember this") scores 0.9, everything else substantive but signal-free lands at 0.4 — worth a second look later (summarization, 167) rather than a confident permanent/not-permanent call today. `importance_score` reuses step 164's own 0.0–1.0 convention directly, so a real caller can pass this decision straight into `MemoryRepository.create(...)` unchanged. Deliberately a pure decision function with no repository dependency — every other real agent that touches persistence (`RetrieverAgent`, `PlanningAgent`) does so outside the generic `run()` contract; actually writing a `Memory` row from a real conversation is a future caller's job once Milestone 6's conversation engine exists to call it from, not something to guess the shape of today. Corrected two now-stale references to `MemoryAgent` as an unimplemented skeleton: `agents/parallel.py`'s own step-154 docstring example (swapped for `ConversationAgent`, still genuinely a skeleton) and `docs/ARCHITECTURE.md`'s "Multi-agent system" section (161) — keeping "as implemented" documentation honest as of this same step, not left stale. Tested: short content rejected, identity-signal content retained at 0.9, case-insensitive signal matching, substantive-but-signal-free content correctly NOT confidently retained, and every decision carrying a real, non-empty reason.
- [x] 166. Add identity-based memory retrieval on conversation start — `memory_retrieval.py:retrieve_memory_for_conversation_start`, real logic for AGENTS.md's own "PERSONALIZED EXPERIENCE"/"PERSONALIZED MEMORY" sections ("When a user provides identifying information... the system should intelligently retrieve previous interactions... Relevant long-term memories"). Combines the two real long-term memory scopes a starting conversation should draw on — user memory and organization memory — via `repositories/memory.py:MemoryRepository` (164), re-sorted and truncated to ONE overall `limit` rather than a per-scope split, so a caller asking for the top 20 most relevant memories gets exactly that across both scopes together, not up to 40. Session-scoped memory deliberately excluded — a NEW conversation has no prior `session_id` of its own yet to look up, which is what "conversation start" means. No `Conversation`/`ConversationSession` model or "start a conversation" endpoint exists yet (Milestone 6), so this has no real HTTP entry point to attach to today — real, tested retrieval logic ready for that future caller, same "ship the real mechanism ahead of its first production caller" precedent this project has followed throughout (`AgentRegistry`, `run_parallel`, `PROVIDERS`). `min_importance` defaults to `agents/memory.py:RETENTION_THRESHOLD` (165) rather than a separately invented number — retrieval and retention share one honest definition of "important enough." Tested: combining and re-sorting both scopes by importance, excluding below-threshold memory, per-user isolation, and the overall limit correctly applying across both scopes together (not per-scope).
- [x] 167. Add memory-summarization background job — `memory_summarization.py:dispatch_memory_summarization`, same Celery task shape `embeddings_pipeline.py:dispatch_embedding_generation` (108, 112) already established (async `_run_*` via `get_worker_session()`, `autoretry_for`/`retry_backoff`), registered in `celery_app.py`'s own `imports` tuple. **The first task in this codebase to call a real LLM provider directly** — `llm/openai.py:OpenAIProvider` (151) sitting real, tested, and unused until now; calling it here (not through an agent — summarization isn't an `Agent.run()`-shaped decision the way `MemoryAgent`'s own retention scoring is) is the same "real code, fails closed without a key, documented environment gap" discipline `embeddings_pipeline.py` already established for embedding generation, applied to a new call rather than new scaffolding. Summarizes a session's short-term memory (163) into a condensed summary, then reuses `agents/memory.py:MemoryAgent` (165) as the retention gate for the SUMMARY itself — "not every conversation should become permanent memory" applies exactly as much to a generated summary as to a raw turn, one real decision logic rather than a second independently invented rule — before writing a retained summary as a `scope="session"` `Memory` row (164). `LLMProviderError` deliberately propagates uncaught so Celery's own `autoretry_for` handles a real transient failure, same as every other external-API-calling task here; this environment's missing `OPENAI_API_KEY` means every real run fails closed the same documented way. `short_term_memory.clear()` runs after a successful summarization attempt either way (retained or not) — the raw turns have been processed once summarized, so keeping them risks re-summarizing the same content on a later dispatch. **Real design tension surfaced while testing:** `MemoryAgent`'s own retention signal phrases (165) are first-person ("my name is", "remember that"), matching raw conversational turns — an LLM-generated summary is naturally third-person, so a summary echoing a turn's content in third person ("the user's name is...") won't trigger the same heuristic a first-person restatement would; tests account for this honestly rather than asserting a false match. Tested: no-op on an untouched session, the real live `LLMProviderError` failure mode against `api.openai.com` with no key configured (turns survive intact for a retry), a retained summary correctly written via `httpx.MockTransport`, a low-value summary not retained but short-term memory still clearing, and the real conversation turns sent alongside the summarization system prompt.
- [x] 168. Add memory expiration/TTL policy engine — `models/memory.py:Memory` gains `expires_at` (nullable `DateTime`). `memory_policy.py:compute_expiration` is the real policy: `importance_score` (164) scales expiration duration rather than inventing a second notion of importance — `>= 0.8` never expires (`None`) since "not everything deserves permanent memory" (AGENTS.md's own "MEMORY LIFECYCLE" section) cuts both ways and something genuinely important should stay permanent; `>= RETENTION_THRESHOLD` (165, the same real bar `memory_retrieval.py` (166) already reuses) gets a 90-day TTL; below that a defensive 7-day floor, a case that shouldn't normally occur since `MemoryAgent` already gates retention at exactly that bar. `expire_stale_memories` is a real, dispatchable Celery task scoped to one tenant per call — the same explicit `tenant_id` argument shape every other real task in this codebase uses — NOT a scheduled/self-triggering job: this codebase never adds Celery Beat or any periodic-task infrastructure, the same "build the task, not speculative scheduling infra" precedent `vector_maintenance.py`'s own step-110 docstring already established; a real deployment's operator triggers this periodically, looping over tenants being an operational concern this project has never solved with a special "iterate every tenant" task. Wired `compute_expiration` into `memory_summarization.py`'s (167) real `Memory`-writing call site, fed the same `importance_score` `MemoryAgent`'s own decision already computed — one real policy governs every `Memory` row this codebase writes, not a second independently invented rule for summaries specifically. Migration generated via real `alembic revision --autogenerate`, applied against the live DB. Tested: the real threshold boundaries (permanent, standard-TTL, low-value-TTL), deleting only past-expired rows while future-expiring and permanent memories survive, and per-tenant scoping.
- [x] 169. Add memory-privacy controls (view/export/delete own memory) — `routers/memory.py`: `GET`/`GET .../export`/`DELETE .../{memory_id}` under `/organizations/{organization_id}/memory`, scoped to the CURRENT user's own `scope="user"` memory only — never organization- or session-scoped memory, neither of which is "theirs" to control this way; deleting one entry is a granular, individual operation, distinct from step 170's own separate, comprehensive "right to erasure." No `require_permission(...)` gate, same self-service shape `routers/mfa.py` already established for "manage my own account" endpoints — real authentication plus real tenant membership (`get_tenant_db`, which already 403s a non-member) is the correct and sufficient bar; requiring a separately-granted permission to see/delete one's OWN memory would be backwards. `DELETE` checks `memory.user_id == user_id` before deleting, not just that the memory exists in this tenant — a 404, not a leak, for another user's memory in the same org, same "ownership check, not just existence check" discipline every other resource-scoping check in this codebase applies. `repositories/memory.py` gains `count_for_user` (fixing a real bug caught before shipping: `total=len(page_items)` would have reported the current page's size, not a true total) and `list_all_for_user` (no `limit`/`min_importance` — genuine "export everything" semantics, ordered chronologically for how a person actually reviews their own history, unlike the importance-first ordering `list_for_user`/`list_for_organization`/`list_for_session` use). Tested against the real FastAPI app: auth required, listing returns only my own user-scoped memories (not organization-scoped or another user's — the "other user" is a real `User` row with a real FK-satisfying id, not a random UUID), export returns everything unpaginated, delete removes my own entry, deleting another user's memory 404s, and non-members are rejected.
- [x] 170. Add memory deletion endpoint (right-to-erasure) — `DELETE /organizations/{organization_id}/memory` (no `memory_id`), the comprehensive erasure step 169's own docstring already distinguished from its per-entry delete: every one of the caller's own `scope="user"` memories in this org, erased in one real, audited operation. Calls `write_audit_log`, unlike step 169's granular delete — a bulk erasure is exactly the consequential event AGENTS.md's own "AUDIT LOGGING" section names specifically ("Memory deletion"), matching the precedent `routers/document.py`/`routers/knowledge_base.py`/`routers/assistant.py` already set for their own delete operations. `resource_type="user_memory"`/`resource_id=user_id` represents the operation acting on the user's memory collection as a whole, since there's no single row it targets; `extra={"deleted_count": N}` records what was actually erased — the one detail an audit entry with no enumerable resource still needs to be useful. `repositories/memory.py:delete_all_for_user` issues one real `DELETE ... RETURNING` statement, not a fetch-then-delete-each loop. **Real, honest gap documented, not silently worked around:** this does NOT reach into Redis short-term memory (163) — short-term entries are keyed by `session_id`, and no `user_id -> session_id` mapping exists anywhere in this codebase yet (no `Conversation`/`ConversationSession` model until Milestone 6), so there is no real way to find every session belonging to a user to erase from Redis today; short-term memory already expires on its own one-hour TTL regardless. Tested: bulk deletion removes only the calling user's own memories (another user's and organization-scoped memory survive untouched), returns the real deleted count, and writes a real, correctly-populated `AuditLog` entry.
- [x] 171. Add tenant-isolation test for memory — `tests/test_memory_tenant_isolation.py`, same two-threat shape `test_retrieval_tenant_isolation.py` already established for `Chunk` (132): raw RLS invisibility across tenants (a real memory row belonging to tenant B, queried from tenant A's context with no app-layer filter of its own, must be invisible), plus each real `MemoryRepository` access point (`get`/`list_for_user`/`list_for_organization`/`list_for_session`) called with a genuinely valid id belonging to ANOTHER tenant — a materially stronger test than the existing random-never-real-UUID 404 endpoint tests. `list_for_user` specifically tests a case `Chunk`'s own isolation tests never had to consider: `User` is a global identity, not tenant-scoped (`ARCHITECTURE.md`'s own tenant hierarchy doc), so the same real user can genuinely belong to more than one organization — this proves tenant A's repository doesn't leak tenant B's memory for that SAME real user, not merely that the user id itself is unguessable. Same flush()-never-commit() discipline as `test_tenant_isolation.py` — nothing here persists once each test's session closes, so no cleanup needed.
- [x] 172. Add memory-observability logging (created/updated/ignored + why) — `memory_observability.py:log_memory_event`, one structured `"memory_lifecycle"` event with an `outcome` field (`created`/`updated`/`ignored`), same "one event name, a status field" shape `agents/tracing.py:traced_run` (153) already established, rather than three separately-named events. Scoped to exactly the roadmap step's own parenthetical, not AGENTS.md's fuller "MEMORY OBSERVABILITY" list (deleted, confidence, importance score, expiration, retrieval, compression, summarization) — "deleted" already has real, dedicated coverage via step 170's audit log, a different mechanism for a different audience (compliance/security, not day-to-day operational visibility), and the rest have no real caller yet. Wired into `memory_summarization.py`'s (167) real retention-decision call site — the one place in this codebase that actually decides whether a `Memory` row gets created, with a real reason `MemoryAgent` (165) already computes; both outcomes are logged (a retained summary logs `"created"`, a rejected one logs `"ignored"`), each carrying the SAME real `decision.reason`, not a second independently invented explanation. `"updated"` is a real, callable outcome with no caller yet — no code path updates an existing `Memory` row today; step 173's own "memory conflict-resolution logic" is what will need it, matching the same "field/capability exists, not every value reachable by real code yet" honesty `memory_type`'s own `"short_term"` value established at step 162. Tested: the logging function itself (all three outcomes, including the none-yet-called `"updated"`), plus real integration proof that `memory_summarization.py`'s retained and ignored paths each emit the correct real event with the correct real reason.
- [x] 173. Add memory conflict-resolution logic — `memory_conflict.py:find_conflicting_memory`, a real, deterministic word-overlap heuristic (normalized Jaccard similarity over word sets), not embedding-based semantic similarity — `Memory` has no embedding column, and adding one (plus computing/storing it on every write) is real new infrastructure this step's own wording doesn't ask for; same "real heuristic, not fake ML" discipline `agents/planning.py`/`agents/memory.py:MemoryAgent` already established for their own decisions. `repositories/memory.py:update_content` is this repository's first real UPDATE path — every prior write was a `create` or a `delete`. Wired into `memory_summarization.py`'s (167) retained-summary branch: before writing, checks this session's existing memories for a conflict; a new summary that meets or beats the conflicting memory's `importance_score` replaces its content via `update_content` and logs `"updated"` — the outcome step 172's own docstring predicted would get its first real caller here — while one that doesn't beat it changes nothing and logs `"ignored"` with a conflict-specific reason, distinct from `MemoryAgent`'s own retention-threshold reasons. **Real bug found and fixed before shipping:** `content_overlap_ratio` originally only stripped leading/trailing punctuation per word, so `"Jordan's"` and `"jordans"` never matched — an internal apostrophe survives edge-stripping; fixed by stripping every non-alphanumeric character instead, caught by the test asserting exactly that normalization. Tested: the overlap heuristic directly (identical/disjoint/partial content, case/punctuation normalization), the repository's new update path, and full integration proof that a genuine conflict updates or ignores correctly depending on which side wins the importance-score comparison — using a seeded score above `MemoryAgent`'s own maximum output (0.9) to construct a real "retained but still loses the conflict" case its 3-tier heuristic can't produce on its own.
- [x] 174. Add memory-lifecycle tests (create→retrieve→expire→delete) — `tests/test_memory_lifecycle.py`, one comprehensive narrative test walking a real `Memory` row through every real stage built across steps 162-173 (create via `MemoryRepository.create`, retrieve via `memory_retrieval.py`, expire via `memory_policy.py`'s real TTL sweep, delete via `MemoryRepository`'s own granular delete), distinct from the many per-component tests already covering each stage in isolation. Two separate memories are used throughout since the roadmap names expiration and deletion as distinct stages — one expires via the real sweep, one is explicitly deleted — proving both real removal mechanisms independently rather than conflating them. **Real gap found and fixed while building the test — the first one to tie expiration (168) and retrieval (166) together:** `repositories/memory.py:list_for_user`/`list_for_organization`/`list_for_session` didn't exclude already-expired-but-not-yet-swept rows. `memory_policy.py:expire_stale_memories` is explicitly a periodic, operator-triggered sweep, not a scheduled job (its own step-168 docstring), so there's a real window where an expired memory still physically exists between when it expires and when the sweep next runs — it shouldn't be usable in that window just because nothing has purged it yet, the sweep's job being storage reclamation, not the sole gatekeeper of visibility. `list_all_for_user` (the export path, 169) deliberately stays unfiltered — "export everything the user owns" should stay honestly complete, not silently hide what hasn't been purged yet. Simulating real time passing for the expiration stage used a real technique, not a shortcut: create with a genuine near-future TTL from `compute_expiration`, confirm it's visible via retrieval, THEN backdate `expires_at` into the past via the real `update_content` path (173) before running the sweep — a test can't literally wait seven real days for its own TTL to lapse.
- [x] 175. Document memory architecture in `docs/ARCHITECTURE.md` — filled in the previously-placeholder "Memory architecture" section, matching the "Multi-agent system" section's own established style (subsections, honest tracked gaps at the end): short-term memory (Redis), long-term memory (`Memory`/`MemoryRepository`), the retention decision (`MemoryAgent`), the summarization pipeline (first code calling a real LLM provider directly), expiration/conflict-resolution policy, and privacy/identity-based retrieval. Updated the Status line: Milestone 5 (Memory System) complete, Milestone 6 (Conversation Engine) next. Documentation-only, no code changes — no lint/type-check/test/CI concern. **Milestone 5 (Memory System) complete — 175/300.**

## Milestone 6 — Conversation Engine (steps 176–200)

- [x] 176. Create `Conversation` model + migration — `models/conversation.py:Conversation` sits under `Assistant` (`assistant_id` FK CASCADE), `user_id` nullable FK `SET NULL` (anonymous widget visitors, and a transcript should outlive the user per `AuditLog.actor_user_id`'s own precedent). Retrofits the real FK `models/memory.py:Memory.session_id` had promised since step 162 (`ondelete="CASCADE"` — a session-scoped memory has no reason to outlive its conversation). Migration `f567d67cde4e` defensively deletes any pre-existing orphaned `memories.session_id` rows before adding the constraint (the new table starts empty, so any non-null value at that point is orphaned by definition) — a real production-safety concern, not just local cleanup, since CI's fresh DB would never hit it but a real deploy against existing data could. RLS verified live. Every Milestone 5 test that persisted a session-scoped `Memory` row with a fake `uuid.uuid4()` session id was updated to create a real `Conversation` first. `test_conversation_model.py` added, genuinely model-only (no repository/endpoint yet). Full suite (689 tests) green.
- [x] 177. Create `Message` model + migration — `models/message.py:Message` sits under `Conversation` (`conversation_id` FK CASCADE), `role`/`content` deliberately mirror `llm/base.py:Message`'s own field names. Deliberately minimal — no citations (step 187), no feedback (189), no per-message token counts, each lands with the step that needs it. RLS verified live. `test_message_model.py` added (create/read, insertion-order-via-created_at, cascade-delete), genuinely model-only. Full suite (692 tests) green.
- [x] 178. Add conversation-create endpoint — `POST .../assistants/{assistant_id}/conversations`, nested one level under `routers/assistant.py`'s hierarchy. No request body (assistant_id from URL, user_id from JWT, tenant_id from context). New `conversation:create` permission granted to every role except `guest`/`viewer` — core product use, not configuration, unlike `assistant:create`'s org_owner/admin/manager-only tier. `get_target_assistant` resolution dependency built inline in `routers/conversation.py` (first consumer; promote later if a second one needs it). `test_rbac_enforcement.py`'s matrix updated. Live-verified against a real running server: signup→org→workspace→KB→assistant→conversation end to end (201, correct body), plus the unauthenticated 401 path. Full suite (698 tests) green.
- [x] 179. Add message-send endpoint wired through orchestrator — `POST .../conversations/{conversation_id}/messages` calls `orchestrator.py:orchestrator.handle()`, the real entry point step 143 built specifically for "a future chat/conversation endpoint." Persists both the user's and the assistant's turns as real `Message` rows via the endpoint's own session; the orchestrator stays fully decoupled, managing its own DB access for retrieval. New module-level `orchestrator` singleton in `orchestrator.py` (same shape `agent_registry`'s own singleton). `get_target_conversation` cross-checks the conversation's `user_id` against the caller (user-owned, not just tenant-owned) — mismatch 404s. New `message:create` permission, same tier as `conversation:create`. Deliberately does not thread message history into `handle()` — that signature stays real and honest (single query in, single response out); history-aware generation isn't this step's literal ask. `test_rbac_enforcement.py` matrix updated. Live-verified against a real running server end to end, including a real "No results found." response from real keyword retrieval against an empty knowledge base. Full suite (703 tests) green.
- [x] 180. Add streaming response support (SSE) — `POST .../conversations/{conversation_id}/messages/stream`, a separate endpoint from `/messages` (genuinely different response shape, `text/event-stream`, no `response_model`). Persists both turns identically to the non-streaming endpoint, then delivers the final response as word-chunked `message` SSE events plus a terminal `done` event carrying the persisted assistant `Message`'s real id/timestamps. Honestly scoped: `orchestrator.handle()` still computes the full response first (no roadmap step through 200 asks for LLM-level token streaming) — this delivers the real, correct SSE transport a future token-streaming source can plug into unchanged. Materializes `MessageRead` before building the generator closure, never touching the ORM object from inside it, since `get_tenant_db` commits (and expires attributes) the instant the endpoint function returns, well before Starlette iterates the `StreamingResponse` body. Live-verified with real `curl -N` against a running server — genuine incremental delivery, not one buffered write. Full suite (706 tests) green.
- [x] 181. Add conversation-state machine (new/active/waiting/processing/completed/archived) — `conversation_state.py:transition()` + `VALID_TRANSITIONS` (a plain dict graph, no state-machine library) validates real moves across the roadmap's own six named states. `Conversation.status` column added (default `"new"`, `server_default` backfill). Only `new → active` (first message sent) is wired into a real caller today — `waiting`/`processing`/`completed` stay real, legal, honestly-unreached states (no human-handoff mechanism, no safe intermediate-commit pattern asked for, no "done" product signal yet, respectively); `archived` is step 184's own explicit job. `test_conversation_state.py` covers the transition graph directly. Live-verified against a real running server: a fresh conversation returns `"new"`, confirmed `"active"` in the DB after the first message. Full suite (713 tests) green.
- [x] 182. Add paginated conversation-history endpoint — `GET .../assistants/{assistant_id}/conversations`, scoped to the caller's own conversations only (`ConversationRepository.list_for_assistant_and_user`, newest first) — "history" reads as personal, matching step 179's own "conversation is user-owned" precedent; an org-wide view isn't asked for. New `conversation:read` permission, deliberately broader than `conversation:create`/`message:create` (includes `viewer`, still excludes `guest`). `test_rbac_enforcement.py` matrix updated. Live-verified against a real running server: two conversations list back newest-first with the correct total. Full suite (716 tests) green.
- [x] 183. Add conversation search endpoint (keyword+semantic) — `Message` gains the same two-mechanism shape `models/chunk.py` already solved (nullable pgvector `embedding` + ivfflat/cosine index for semantic, DB-computed `search_vector` tsvector + GIN index for keyword) — reused, not reinvented. `message_embedding.py:dispatch_message_embedding` computes each message's embedding asynchronously via Celery, dispatched after every `Message.create()` (both turns). Two separate endpoints, `POST .../conversations/search/keyword` and `.../search/semantic` — same "two genuinely different mechanisms" precedent `routers/retrieval.py`'s own dense/keyword/hybrid split established — both scoped to the caller's own conversations, reusing `conversation:read`. `search_semantic` fails closed with a 500 in this environment (no real `OPENAI_API_KEY`), the same accepted, documented gap `dense_search` already has. Live-verified against a real running server (real keyword match, real documented 500) plus a real worker confirming task registration; also purged a large stale Celery queue backlog accumulated from this session's own many live-verify runs. Full suite (723 tests) green.
- [x] 184. Add rename/pin/archive/delete conversation endpoints — three endpoints, not four: `PATCH .../{conversation_id}` folds rename+pin into one `model_fields_set`-driven partial update (`ConversationUpdate`), same shape `routers/security_settings.py`'s own PATCH already established. `POST .../{conversation_id}/archive` is its own endpoint (a real, validated `conversation_state.py:transition()`, not a raw field write) — archiving an already-archived conversation 409s, matching `routers/invitation.py`'s own revoke precedent. `DELETE .../{conversation_id}` is a real hard delete, cascading to `Message` rows. `Conversation` gained `title`/`is_pinned` (the gap step 176 flagged for this step). New `conversation:update`/`conversation:delete` permissions, same broad tier as `conversation:create`/`message:create`. `test_rbac_enforcement.py` matrix updated. Live-verified against a real running server: rename+pin, archive, double-archive's real 409, delete, double-delete's real 404. Full suite (728 tests) green.
- [x] 185. Add markdown-rendering support in message schema — `message_rendering.py:render_markdown` (Python-Markdown, BSD-3-Clause) converts stored content to HTML, sanitized with `nh3` (MIT) — verified live that a raw `<script>` tag survives unrendered markdown output completely unescaped, a real XSS surface once a real LLM eventually generates message content. Zero markdown extensions enabled — fenced code/tables are step 186's own job, using the same renderer. `MessageRead.content_html` is a Pydantic `computed_field`, rendered fresh on every read (no staleness-prone caching). Live-verified against a real running server: a message with both markdown and an embedded `<script>` persisted untouched, rendered correctly, script stripped. Full suite (737 tests) green.
- [x] 186. Add code-block/table formatting support — enables Python-Markdown's `fenced_code`/`tables` extensions in the same `message_rendering.py` renderer step 185 built (no new module). Real, live-confirmed gap found+fixed: `nh3`'s default sanitization strips ANY `class` attribute, including the `language-xxx` hint `fenced_code` emits on `<code>` (needed for a future frontend syntax highlighter) — fixed with a narrow `attribute_filter` allowing `class` on `<code>` only when it starts with `"language-"`, real defense in depth verified against Python-Markdown's own fence parser (which never emits anything else there). `table`/`thead`/`tbody`/`tr`/`th`/`td` needed no extra allowance — nh3's own default tag list already covers them, confirmed live. Live-verified against a real running server: a message with a real fenced Python code block and a real table both rendered correctly through the actual persisted content. Full suite (740 tests) green.
- [x] 187. Add citation display in chat responses — `orchestrator.py:Orchestrator.handle()` now returns `OrchestratorResult` (`response` + `chunks: list[RetrievedChunk]`) instead of a bare `str`, a real, motivated widening step 179's own restraint explicitly deferred until a step actually needed it. `Message` gains a `citations` JSONB column (list, default `[]`). `routers/conversation.py` builds real `citations.py:Citation` objects right after `orchestrator.handle()` returns, fetching each unique `Document`/`KnowledgeBase` at most once per request, stored as `CitationRead.model_dump(mode="json")` dicts (sidesteps `Citation`'s own `uuid.UUID` fields not being directly JSONB-serializable). User messages never get citations — nothing retrieves on their behalf. Live-verified against a real running server: a real seeded `Chunk` produced a real, correctly-populated citation through the actual HTTP endpoint. Full suite (743 tests) green.
- [x] 188. Add regenerate-response endpoint — `POST .../conversations/{conversation_id}/messages/{message_id}/regenerate` updates the existing assistant `Message` row in place (no versioning/branching — nothing through step 200 asks for parallel response versions). `get_target_message` requires `role == "assistant"`, 404s otherwise. `MessageRepository.get_preceding_user_message` finds the real originating query by nearest-earlier `created_at`. Re-dispatches embedding, rebuilds citations, reuses `message:create`. **Real bug found and fixed by this step's own tests**: Postgres's `now()` returns transaction-start time, not per-statement — since `send_message` writes both messages in one uncommitted transaction, they get byte-identical `created_at` values (confirmed live with a real insert + real sleep), so the original strict `<` comparison silently matched nothing; fixed to `<=`, safe under the one-user-then-one-assistant-per-call invariant. Live-verified against a real running server. Full suite (748 tests) green.
- [x] 189. Add feedback endpoint (helpful/not helpful/incorrect/etc.) — `Message.feedback_type` (nullable str), the real in-scope taxonomy AGENTS.md's own "FEEDBACK COLLECTION" section names for a text response (`voice_quality` deliberately excluded — different modality). `PUT`/`DELETE .../messages/{message_id}/feedback` set/clear it, reusing `get_target_message` (assistant-only) and `message:create`. **Real bug found and fixed by this step's own tests, confirmed with a live diagnostic**: `Message.search_vector` is `Computed` from `content`, so Postgres's `RETURNING` only refreshes it (and `updated_at`, bundled) when `content` itself changes in that `UPDATE` — `regenerate_response` gets this for free, but `set_message_feedback` (mutates only `feedback_type`) left both genuinely expired after `flush()`, causing a real `MissingGreenlet` in Pydantic's synchronous serialization. Fixed with an explicit `session.refresh()`; confirmed `Conversation` (no `Computed` column) doesn't share this risk. Live-verified against a real running server. Full suite (756 tests) green.
- [x] 190. Add follow-up-question suggestion generation — `follow_up_questions.py:generate_follow_up_questions` calls a real LLM provider directly (`OpenAIProvider`, second such caller after `memory_summarization.py`). `POST .../messages/{message_id}/follow-up-questions`, a separate endpoint from message-send so `send_message`/`send_message_streaming` keep working in every environment (including this one, no real `OPENAI_API_KEY`) — same "separate endpoint, own accepted environment-specific gap" precedent `dense_search`/`search_semantic` already established. Not persisted — a real-time UI nicety, no archival value. Reuses `get_preceding_user_message` (188). Basic list-marker stripping handles a real, common LLM compliance failure mode. Live-verified against a real running server: correctly fails closed with a real 500, same honest behavior as `dense_search`/`search_semantic`. Full suite (766 tests) green.
- [x] 191. Add conversation-export endpoint (JSON/markdown) — `GET .../conversations/{conversation_id}/export?format=json|markdown`, one endpoint with a query param (not a mechanism split like search/keyword vs semantic — export is one data fetch, two serializations, closer to `ContextSearchRequest.strategy`'s precedent). New `MessageRepository.list_for_conversation` (nothing needed the full ordered transcript until now). Both formats build from the same `ConversationRead`/`MessageRead` data. Real `Content-Disposition: attachment` header on both — AGENTS.md's own "Download conversations" phrasing. Reuses `conversation:read`. Live-verified against a real running server: both JSON and Markdown produced correct real output with the right headers. Full suite (773 tests) green.
- [x] 192. Add anonymous-session support (pre-auth visitors) — a genuinely separate public router (`routers/public_conversation.py`), keyed by `assistant_id` alone (`/public/assistants/{assistant_id}/...`), not a branch of the authenticated `/organizations/...` hierarchy an anonymous visitor can't satisfy. Public reachability is opt-in per assistant (`Assistant.is_public`, default `False`, settable only at creation time). New narrowly-scoped permissive RLS policy `assistant_by_id` (same shape `invitation_by_token`/074 already established) lets an anonymous caller look up an Assistant by ID before any tenant context exists; `is_public=True` is still checked in the application layer on top of that. Ownership of an anonymous `Conversation` is proven by a real signed JWT (`type "anonymous_session"`, same `mfa_pending`-ticket precedent) scoped to exactly one conversation. `message_processing.py:generate_assistant_reply` (promoted out of `routers/conversation.py`'s own `send_message`, now a second real caller) does the real message-send work identically to the authenticated flow. Live-verified against a real running server: non-public 404s, public issues a real anonymous session with no auth header, a real message worked end to end through the real orchestrator, missing token 401s. Full suite (782 tests) green, including real negative/security tests.
- [x] 193. Add user-identification-triggered memory reconnection — `POST .../conversations/claim` on the authenticated router, the real "trigger" moment this codebase actually has for AGENTS.md's own "USER IDENTIFICATION" section ("when a user provides identifying information... reconnect them with their previous history... relevant memories"): an anonymous visitor from step 192's public router who then signs in. Takes only `anonymous_token` in the body — no separate `conversation_id`, since the token itself already names exactly one conversation (`auth/jwt.py:decode_anonymous_session_token`), avoiding a redundant second identifier to cross-check. Requires a real authenticated caller (normal JWT + RBAC + tenant context); rejects (404) a conversation that isn't this assistant's or is already claimed. Reuses `conversation:update`, not a new permission. Calls `memory_retrieval.py:retrieve_memory_for_conversation_start` (166, unwired until now) with the newly-real `tenant_id`/`user_id` and returns the reconnected memories directly in the response rather than threading them into `orchestrator.handle()` — no chat UI exists yet to consume a prompt-injected version, and AGENTS.md's own example list reads as things a client surfaces, not a hidden prompt detail. Deliberately scoped to memory only, not conversation-history reconnection — `list_conversations` (182) already covers that once a conversation is claimed. **Real bug found and fixed, same root cause as step 189's**: `Conversation.updated_at` (`onupdate=func.now()`, server-computed) is left expired once autoflush fires the `UPDATE` — here triggered by `retrieve_memory_for_conversation_start`'s own SELECT queries running right after the ownership mutation, unlike `update_conversation`/`archive_conversation` which never trigger an autoflush before their own `model_validate()` call. Fixed with the same `session.flush()`+`session.refresh()` pair step 189 already established. Live-verified against a real running server: real claim (200, memory reconnected, `updated_at` correctly refreshed), double-claim 404, invalid-token 401, claimed conversation reachable through the normal authenticated list. Full suite (789 tests) green.
- [x] 194. Build chat UI shell in `apps/web` (message list, input, streaming render) — the first real step to touch `apps/web` since Milestone 0's scaffold. No dashboard auth UI exists yet (`docs/ROADMAP.md` step 233, "auth-gated layout," is well after this one), so this shell talks to the real backend through step 192's own zero-auth anonymous/public conversation flow — the same reasoning that made that flow worth building, and the same reason a future embed script or dashboard "open chat" link can both plug into this page's `?assistantId=` contract unchanged. New `app/chat/page.tsx` (reads `assistantId` from the query string), `components/chat/{ChatShell,MessageList,MessageInput}.tsx`, `lib/{types,api}.ts`. Streams the assistant's reply progressively (word-by-word) while the request is in flight, then swaps to the final message's server-rendered `content_html` (Python-Markdown + nh3, steps 185/186) once the `done` SSE event arrives — reuses the backend's own sanitized-HTML pipeline instead of re-implementing markdown parsing client-side. Citations render inline once the reply completes. **Real backend gap this step needed, not the frontend itself:** `routers/public_conversation.py` had no streaming endpoint — only the authenticated router (180) did — so `POST .../{conversation_id}/messages/stream` was added there too, built on the same `message_processing.py:generate_assistant_reply`/new `build_message_stream` pair the authenticated endpoint was refactored to use (removing its own duplicated inline persistence logic once a second real caller needed the identical SSE shape). **Second real bug found live, not by inspection:** the API had no CORS middleware at all — a real cross-origin `fetch` from `localhost:3000` to `localhost:8000` was silently blocked by the browser. Fixed with a wildcard-origin, `allow_credentials=False` `CORSMiddleware` in `main.py` — safe specifically because every real API route in this codebase authenticates via a Bearer token attached by caller JS, never a cookie (confirmed: the only cookie anywhere is `routers/oauth.py`'s CSRF state cookie, on a top-level redirect flow CORS never gates), and it's also what the real embeddable-widget channel (192) needs anyway, since a widget's embedding origin can never be known in advance. Live-verified in a real browser against the real running API: anonymous session created, a real multi-turn conversation sent and received via genuine SSE streaming, the real seeded citation rendered, conversation history preserved across turns. 791 tests green (all passing once a large batch of pre-existing, unrelated leftover test-org rows from earlier steps — not caused by this step — were cleaned out of the local Postgres instance).
- [x] 195. Add typing-indicator UI — new `components/chat/TypingIndicator.tsx` (three CSS-animated dots, `role="status"`/`aria-label="Assistant is typing"`, `prefers-reduced-motion` respected), a genuinely distinct capability from step 194's own streaming render — matches AGENTS.md's own "CHAT EXPERIENCE" section, which lists "Streaming responses" and "Typing indicators" as separate line items, not one feature. `MessageList.tsx` renders it specifically for the real gap between sending a message and the first SSE chunk arriving (`message.pending && message.content === ""`) — genuine backend work time, since `orchestrator.handle()` computes the full response before apps/api's streaming endpoint emits anything (180's own docstring) — then swaps to the real streamed text once content starts arriving, same as before. Verified via a real production build (typecheck+lint clean) and by exercising the real send flow against the real running backend twice: confirmed the pre-content gap genuinely exists (assistant heading rendered with no content yet, right after send) and that the full flow still completes correctly end to end, no regressions. The exact indicator frame itself was too fast to catch via automated screenshotting against this tiny local single-chunk KB (confirmed via real network timing — the round trip resolves faster than this tool's own call latency); verified by code-path correctness and direct reasoning instead, the same "caught and avoided via reasoning, not hit live" precedent step 180's own SSE footgun already established for this project. No backend changes this step.
- [x] 196. Add conversation-list sidebar UI — new `lib/conversationStore.ts` (localStorage-backed, keyed per assistant) + `components/chat/ConversationSidebar.tsx`, deliberately NOT wired to apps/api's own `list_conversations` (182): that endpoint requires a real authenticated JWT + Membership, which nothing in `apps/web` can supply yet (dashboard auth UI is step 233, well after this one), and the anonymous flow this chat UI shell (194) actually runs on has no server-side "my conversations" concept at all by design — an anonymous session is proven only by possessing its own access token, there's no real identity to list conversations for. "Your recent chats on this device" is the honest capability this surface offers today, the same pattern real embeddable chat widgets use for anonymous visitors; step 233's own dashboard gets a real, server-backed sidebar once it exists. Each stored entry keeps the full message list (not just the id/token), so switching to a past conversation restores its history entirely client-side — no backend message-list endpoint exists for the public router (and adding one was out of this step's real scope). A conversation only earns a sidebar entry once it has a real message, matching how most real chat products treat an unused "new chat." `ChatShell` now resumes the most recently active conversation on mount (serves AGENTS.md's own "conversation continuity" line across page reloads, not just within one tab), and `app/chat/page.tsx` gained `key={assistantId}` so a future assistant switch remounts cleanly rather than carrying stale sidebar state. Two `react-hooks/set-state-in-effect` lint findings (React's newer, stricter rule) were suppressed with narrow, commented `eslint-disable` lines rather than restructured away — both are the standard, unavoidable "sync from a browser-only API after mount, safe for SSR" shape the rule doesn't actually have a better answer for. Live-verified against a real running server: empty-state sidebar, a sent message correctly creating a sidebar entry, a second "new conversation" correctly listed separately, switching back to the first restoring its full history (including its real citation) without any network refetch, and a page reload correctly auto-resuming the active conversation. No backend changes this step.
- [x] 197. Add conversation-search UI — new `lib/conversationStore.ts:searchConversations` + a search `<input>` in `ConversationSidebar.tsx`, filtering this device's own stored conversation history (196) rather than calling apps/api's real `search_keyword`/`search_semantic` endpoints (183) — those search across the CALLER's OWN conversations via an authenticated request, which this anonymous chat UI shell still has no way to make (the same "no dashboard auth UI yet" constraint 194/196 already documented, now applying a third time). A conversation matches if its title OR any message's content (case-insensitive substring) contains the query — genuinely searches full message content, not just titles, confirmed live by matching text that only ever appeared in an assistant reply, never the (user-message-derived) title. The search box only renders once at least one conversation exists (no empty search UI over an empty list). A distinct "No matching conversations." empty state, separate from "No conversations yet." — searching an empty history and searching-with-no-results are different, honest situations. Live-verified against a real running server: two real conversations with different content, search narrowing to one match by title, narrowing by content-only text, a genuine no-matches state, and clearing the query correctly restoring the full list. No backend changes this step.
- [x] 198. Add Playwright e2e test: send message → receive streamed response — the first real automated test coverage for `apps/web` (`web-tests.yml`'s own comment had been saying "no unit/e2e test suite exists yet" since Milestone 0). New `apps/web/e2e/chat.spec.ts` is a genuine, unmocked end-to-end test: a real browser drives the real `/chat` UI shell (194), which talks to a real running apps/api instance the test itself provisions via direct HTTP calls (signup → login → org → workspace → knowledge base → public assistant, the same chain every apps/api endpoint test already builds). Deliberately uploads no document — `orchestrator.py`'s own honest "No results found." reply for an empty knowledge base (143) is itself a real, deterministic response, and needs none of the async extraction/chunking/embedding pipeline to produce, keeping this test's own setup minimal and fast. Asserts at TWO levels: the raw SSE response itself (real `text/event-stream` content-type, more than one `event: message` chunk, a terminal `event: done`) proving genuine incremental delivery at the protocol level, not just an eventual UI state; and the rendered UI (both the user's message and the real reply visible in the message list). **Real bug this test caught immediately, not by inspection:** the sidebar (196) shows a conversation's title (derived from the first user message) right next to the message list showing that same text, so an unscoped `page.getByText(...)` matched both and failed Playwright's strict-mode ambiguity check — fixed by adding `data-testid="message-list"` to `MessageList.tsx` and scoping the assertions to it, a real, permanent locator-precision fix, not a workaround. `web-tests.yml`'s existing single job (its own comment: "this job gains a real test step alongside build... instead of replacing it") now also spins up Postgres+Redis (no MinIO/ClamAV — this test's own scope never uploads a file, so nothing in its path touches storage or the virus scanner, even though Redis is still required: message-send synchronously calls `message_embedding.py`'s `.delay()`, which needs a reachable Celery broker to publish to even with no worker consuming it), runs real migrations, starts a real `uvicorn` instance, installs Playwright's Chromium, then runs the suite — uploading the HTML report as an artifact on failure. Verified locally end-to-end against the real stack (API + Postgres + Redis + a real `next dev` server), passing consistently across repeated runs after the locator fix.
- [ ] 199. Add per-tenant rate limiting on message-send
- [ ] 200. Document conversation lifecycle in `docs/ARCHITECTURE.md`

## Milestone 7 — Embeddable Widget (steps 201–215)

- [ ] 201. Scaffold `apps/widget` (vanilla TS, minimal deps)
- [ ] 202. Add widget build pipeline (single bundled JS output)
- [ ] 203. Add widget config loader (org/assistant ID from script tag)
- [ ] 204. Add widget launcher button UI
- [ ] 205. Add widget chat window UI (shares logic via `packages/shared`)
- [ ] 206. Add widget theming (colors/fonts/logo/position)
- [ ] 207. Add embed-code generator in dashboard
- [ ] 208. Add widget CORS/allowed-domains config per org
- [ ] 209. Add widget CDN deployment pipeline
- [ ] 210. Add widget versioning strategy (pinned script version)
- [ ] 211. Add widget dark/light mode support
- [ ] 212. Add widget mobile-responsive layout
- [ ] 213. Add widget bundle-size budget check in CI
- [ ] 214. Add widget smoke test (loads on fixture HTML page, sends message)
- [ ] 215. Document widget embedding process in docs

## Milestone 8 — Voice Platform (steps 216–232)

- [ ] 216. Add speech-provider abstraction interface (STT+TTS)
- [ ] 217. Implement Whisper STT provider
- [ ] 218. Implement TTS provider
- [ ] 219. Create `VoiceSession` model + migration
- [ ] 220. Add voice-session-start endpoint
- [ ] 221. Add streaming audio ingestion (websocket)
- [ ] 222. Add streaming TTS audio output
- [ ] 223. Add silence detection
- [ ] 224. Add voice-activity detection
- [ ] 225. Add barge-in/interruption handling
- [ ] 226. Wire Voice Agent into orchestrator (shares Conversation Agent intelligence)
- [ ] 227. Add voice-latency instrumentation
- [ ] 228. Add voice-session-end + transcript persistence
- [ ] 229. Add voice widget UI (mic button, waveform indicator)
- [ ] 230. Add voice tenant-isolation test
- [ ] 231. Add voice quality/latency benchmark script
- [ ] 232. Document voice architecture in `docs/ARCHITECTURE.md`

## Milestone 9 — Admin Dashboard & Analytics (steps 233–250)

- [ ] 233. Scaffold dashboard shell in `apps/web` (auth-gated layout, nav)
- [ ] 234. Add org-settings page (branding, security settings)
- [ ] 235. Add workspace-management UI
- [ ] 236. Add knowledge-base management UI (upload, list, status)
- [ ] 237. Add document detail view (chunking decision, quality warnings)
- [ ] 238. Add assistant-builder UI (instructions, knowledge access, agent config)
- [ ] 239. Add user/role-management UI
- [ ] 240. Add invitation-management UI
- [ ] 241. Add API-key management UI + endpoints
- [ ] 242. Add Analytics Agent skeleton (aggregates usage metrics)
- [ ] 243. Add conversation-analytics endpoint + dashboard chart
- [ ] 244. Add knowledge-health dashboard (duplicates, low-confidence, unused docs)
- [ ] 245. Add agent-performance dashboard
- [ ] 246. Add usage-tracking (messages/voice-minutes/uploads/storage per org)
- [ ] 247. Add audit-log viewer UI
- [ ] 248. Add system-health dashboard (queue depth, worker status, provider status)
- [ ] 249. Add platform-admin layer (cross-org super-admin views)
- [ ] 250. Add dashboard e2e test (login → upload doc → processed → chat → see analytics)

## Milestone 10 — Security & Observability (steps 251–266)

- [ ] 251. Harden Safety Agent: explicit separation of retrieved content from system instructions
- [ ] 252. Add strict schema validation on every API route (no raw dict passthrough)
- [ ] 253. Document secrets-management approach (env-based now, vault path documented)
- [ ] 254. Add encryption-at-rest for sensitive columns (API keys, tokens)
- [ ] 255. Add security-event audit logging (failed logins, permission denials, cross-tenant attempts)
- [ ] 256. Add OpenTelemetry tracing across API+workers
- [ ] 257. Add Prometheus-compatible metrics export
- [ ] 258. Add centralized error tracking (Sentry or equivalent)
- [ ] 259. Refine rate-limit + abuse-detection middleware
- [ ] 260. Add automated dependency-vulnerability scanning in CI
- [ ] 261. Add SAST scanning in CI
- [ ] 262. Add security test suite (auth bypass, cross-tenant, injection attempts)
- [ ] 263. Add incident-response runbook doc
- [ ] 264. Add `SECURITY.md` (responsible disclosure policy)
- [ ] 265. Add backup automation + restore-drill script
- [ ] 266. Document security architecture in `docs/ARCHITECTURE.md`

## Milestone 11 — Infrastructure & Deployment (steps 267–280)

- [ ] 267. Add production Dockerfiles (multi-stage, non-root user)
- [ ] 268. Add `docker-compose.prod.yml` reference deployment
- [ ] 269. Add self-hosted deployment documentation
- [ ] 270. Add CI/CD: build+push images on merge to main
- [ ] 271. Add staging-deployment workflow
- [ ] 272. Add production-deployment workflow with manual approval gate
- [ ] 273. Add health-check + readiness probes
- [ ] 274. Add horizontal-scaling config for API/workers
- [ ] 275. Add CDN config for widget assets
- [ ] 276. Add environment-config documentation (all env vars)
- [ ] 277. Add rollback-procedure documentation
- [ ] 278. Add infra-as-code skeleton (Terraform/Pulumi) for one reference cloud target
- [ ] 279. Add uptime/alerting integration
- [ ] 280. Document infra architecture in `docs/ARCHITECTURE.md`

## Milestone 12 — Open Source Readiness / Public Beta (steps 281–295)

- [ ] 281. Finalize README (quickstart, architecture diagram, screenshots)
- [ ] 282. Finalize `CONTRIBUTING.md` (dev setup, PR process, coding standards)
- [ ] 283. Add issue templates (bug/feature/research)
- [ ] 284. Add PR template
- [ ] 285. Set up GitHub labels
- [ ] 286. Set up GitHub milestones matching this roadmap
- [ ] 287. Add public-facing roadmap summary to README
- [ ] 288. Add `FAQ.md`
- [ ] 289. Document plugin/extension points
- [ ] 290. Add OpenAPI-generated API reference docs
- [ ] 291. Add demo knowledge base + assistant for new contributors
- [ ] 292. Add `CHANGELOG.md` + semantic-versioning setup
- [ ] 293. Tag `v0.1.0-beta` release
- [ ] 294. Publish public beta announcement (GitHub Discussions)
- [ ] 295. Collect and triage first round of community feedback

## Milestone 13 — v1.0 Release (steps 296–300)

- [ ] 296. Address beta feedback backlog
- [ ] 297. Run full security review pass
- [ ] 298. Run full performance benchmark pass
- [ ] 299. Final documentation review
- [ ] 300. Tag `v1.0.0` release

---

## Notes

- This roadmap is a living document. Update it as scope is refined — see `AGENTS.md` §13 "Roadmap Management."
- Each step above should become exactly one commit, pushed immediately after verification (`AGENTS.md` §10, §14).
- Steps deliberately end each subsystem with a "document in `docs/ARCHITECTURE.md`" step — do not skip these; future contributors rely on them.
- Provider-specific steps (OpenAI, Whisper, Google OAuth, etc.) are the *first* implementation behind an abstraction interface, not a permanent commitment — swapping/adding providers later is an additive step, not a rewrite.
