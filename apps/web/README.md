# apps/web

Next.js admin dashboard — see [`/docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md).

```bash
pnpm --filter @agentforge/web run dev
```

`make web-dev` from the repo root does the same thing. See the repo root [`README.md`](../../README.md#quickstart-local-development) for the full local setup.
